# 分布式系统项目文档

## 项目概述

本项目是一个基于Spring Boot的分布式系统，实现了用户注册登录、商品管理、订单管理、分布式缓存、读写分离、负载均衡等功能。

## 技术栈

- **后端框架**：Spring Boot 2.7.15
- **持久层**：MyBatis
- **数据库**：MySQL
- **缓存**：Redis
- **消息队列**：Kafka
- **搜索引擎**：ElasticSearch
- **负载均衡**：Nginx
- **容器化**：Docker

## 需求实现情况

### 1. 系统设计文档

#### 1.1 系统架构草图

- **服务拆分**：
  - 用户服务：处理用户注册、登录等功能
  - 商品服务：处理商品管理、库存管理等功能
  - 订单服务：处理订单创建、查询等功能
  - 库存服务：处理商品库存的增减

#### 1.2 各服务API接口

- **用户服务**：
  - POST /api/auth/register - 用户注册
  - POST /api/auth/login - 用户登录

- **商品服务**：
  - GET /api/products/{id} - 获取商品详情
  - POST /api/products/decrease-stock - 减少商品库存
  - POST /api/products/decrease-seckill-stock - 减少秒杀库存

- **订单服务**：
  - POST /api/orders - 创建订单
  - GET /api/orders/{id} - 获取订单详情
  - GET /api/orders/user/{userId} - 获取用户订单列表

- **搜索服务**：
  - GET /api/search - 搜索商品

#### 1.3 数据库ER图

- **用户表**：id, username, password, email, create_time
- **商品表**：id, name, description, price, stock, seckill_stock, create_time, update_time
- **订单表**：id, user_id, product_id, quantity, total_price, status, create_time, update_time

### 2. 环境准备

- **初始化项目代码仓库**：已完成
- **搭建基础开发环境**：Spring Boot + MyBatis + MySQL
- **搭建项目代码框架**：实现了用户注册登录功能

### 3. 容器环境

- **DockerFile**：已配置
- **docker-compose文件**：已配置，包含数据库、后端服务、Nginx、Redis、Kafka等服务

### 4. 负载均衡

- **后端服务**：启动多个实例，分别开启8081和8082端口
- **Nginx**：配置了负载均衡，支持轮询、加权轮询、IP哈希等算法

### 5. 动静分离

- **前端Html文件**：已创建
- **Nginx配置**：已配置动静分离，静态资源直接处理，动态请求转发到后端

### 6. 分布式缓存

- **Redis缓存**：已集成，实现了商品详情页缓存
- **缓存问题处理**：
  - 缓存穿透：缓存空值，设置较短的过期时间
  - 缓存击穿：使用本地锁防止并发请求穿透到数据库
  - 缓存雪崩：添加随机过期时间，避免缓存同时失效

### 7. 读写分离

- **MySQL读写分离**：已配置主从数据源
- **动态数据源切换**：通过AOP切面根据方法名切换数据源

### 8. ElasticSearch

- **ElasticSearch集成**：已配置
- **商品搜索功能**：已实现

### 9. 消息队列

- **Kafka集成**：已配置
- **订单异步处理**：已实现，订单创建后发送消息到Kafka

### 10. 秒杀功能

- **Redis缓存库存**：使用Redis原子操作实现秒杀库存的预扣减
- **Lua脚本实现原子化操作**：使用Lua脚本确保库存检查、扣减和用户秒杀标记的原子性
- **Kafka异步处理订单**：订单创建后发送消息到Kafka，异步处理订单相关逻辑
- **雪花算法生成订单ID**：已实现
- **幂等性**：防止重复下单，同一用户同一商品只能秒杀一次
- **数据一致性**：保证最终库存不超卖，订单数据完整

## 核心代码实现

### 1. 分布式缓存实现

```java
@Override
public Product getProductById(Long id) {
    String cacheKey = PRODUCT_CACHE_KEY_PREFIX + id;

    // 1. 尝试从缓存获取
    Product product = (Product) redisTemplate.opsForValue().get(cacheKey);
    if (product != null) {
        return product;
    }

    // 2. 缓存穿透处理：检查是否为缓存空值
    if (Boolean.TRUE.equals(redisTemplate.hasKey(cacheKey + ":null"))) {
        return null;
    }

    // 3. 缓存击穿处理：使用锁防止并发请求穿透到数据库
    if (lock.tryLock()) {
        try {
            // 再次检查缓存，防止其他线程已经更新了缓存
            product = (Product) redisTemplate.opsForValue().get(cacheKey);
            if (product != null) {
                return product;
            }

            // 4. 从数据库查询
            product = productMapper.findById(id);

            // 5. 更新缓存
            if (product != null) {
                // 缓存雪崩处理：添加随机过期时间
                long expireTime = CACHE_EXPIRE_TIME + (long) (Math.random() * 300);
                redisTemplate.opsForValue().set(cacheKey, product, expireTime, TimeUnit.SECONDS);
            } else {
                // 缓存穿透处理：缓存空值
                redisTemplate.opsForValue().set(cacheKey + ":null", "", CACHE_NULL_EXPIRE_TIME, TimeUnit.SECONDS);
            }
        } finally {
            lock.unlock();
        }
    } else {
        // 6. 其他线程获取锁失败，等待后重试
        try {
            Thread.sleep(100);
        } catch (InterruptedException e) {
            Thread.currentThread().interrupt();
        }
        return getProductById(id);
    }

    return product;
}
```

### 2. 读写分离实现

```java
@Aspect
@Component
public class DataSourceAspect {

    @Pointcut("execution(* com.example.distributed.system.mapper.*.select*(..)) || execution(* com.example.distributed.system.mapper.*.get*(..)) || execution(* com.example.distributed.system.mapper.*.find*(..))")
    public void readPointcut() {}

    @Pointcut("execution(* com.example.distributed.system.mapper.*.insert*(..)) || execution(* com.example.distributed.system.mapper.*.update*(..)) || execution(* com.example.distributed.system.mapper.*.delete*(..))")
    public void writePointcut() {}

    @Before("readPointcut()")
    public void setReadDataSource() {
        DataSourceConfig.setDataSourceType(DataSourceConfig.DataSourceType.SLAVE);
    }

    @Before("writePointcut()")
    public void setWriteDataSource() {
        DataSourceConfig.setDataSourceType(DataSourceConfig.DataSourceType.MASTER);
    }

    @After("readPointcut() || writePointcut()")
    public void clearDataSource() {
        DataSourceConfig.clearDataSourceType();
    }
}
```

### 3. 订单创建实现

```java
@Override
public Long createOrder(Long userId, Long productId, Integer quantity) {
    // 1. 幂等性检查：防止重复下单
    String orderKey = "order:user:" + userId + ":product:" + productId;
    if (Boolean.TRUE.equals(redisTemplate.hasKey(orderKey))) {
        throw new RuntimeException("您已经购买过该商品");
    }

    // 2. 检查商品库存
    Product product = productMapper.findById(productId);
    if (product == null || product.getStock() < quantity) {
        throw new RuntimeException("商品库存不足");
    }

    // 3. 减少库存
    int newStock = product.getStock() - quantity;
    productMapper.updateStock(productId, newStock);

    // 4. 生成订单ID
    Long orderId = generateOrderId();

    // 5. 创建订单
    Order order = new Order();
    order.setId(orderId);
    order.setUserId(userId);
    order.setProductId(productId);
    order.setQuantity(quantity);
    order.setTotalPrice(product.getPrice().multiply(new BigDecimal(quantity)));
    order.setStatus(1); // 订单状态：1-待支付
    order.setCreateTime(new Date());
    order.setUpdateTime(new Date());

    // 6. 保存订单到数据库
    orderMapper.insert(order);

    // 7. 设置幂等性标记
    redisTemplate.opsForValue().set(orderKey, "1", 24, TimeUnit.HOURS);

    // 8. 发送订单创建消息到Kafka，异步处理
    if (kafkaTemplate != null) {
        try {
            kafkaTemplate.send("order-create", orderId.toString());
        } catch (Exception e) {
            // 记录错误日志，但不影响订单创建
            System.err.println("发送Kafka消息失败: " + e.getMessage());
        }
    }

    return orderId;
}
```

### 4. 秒杀功能实现

#### 4.1 Lua脚本实现原子化操作

```lua
-- 秒杀库存检查和扣减的Lua脚本
-- 检查库存是否充足，充足则扣减，否则返回失败

-- 参数：
-- KEYS[1]：库存键
-- KEYS[2]：用户秒杀标记键
-- ARGV[1]：用户ID
-- ARGV[2]：商品ID

-- 1. 检查用户是否已经参与过秒杀
if redis.call('exists', KEYS[2]) == 1 then
    return -1 -- 用户已经秒杀过
end

-- 2. 检查库存是否充足
local stock = tonumber(redis.call('get', KEYS[1]))
if not stock or stock <= 0 then
    return 0 -- 库存不足
end

-- 3. 扣减库存
redis.call('decr', KEYS[1])

-- 4. 设置用户秒杀标记
redis.call('set', KEYS[2], '1', 'EX', 86400) -- 24小时过期

return 1 -- 秒杀成功
```

#### 4.2 秒杀下单实现

```java
public Long createSeckillOrder(Long userId, Long productId) {
    // 1. 准备Redis键
    String stockKey = "seckill:stock:" + productId;
    String orderKey = "seckill:order:user:" + userId + ":product:" + productId;

    // 2. 加载Lua脚本
    DefaultRedisScript<Long> script = new DefaultRedisScript<>();
    script.setScriptSource(new ResourceScriptSource(new ClassPathResource("lua/seckill.lua")));
    script.setResultType(Long.class);

    // 3. 执行Lua脚本
    List<String> keys = new ArrayList<>();
    keys.add(stockKey);
    keys.add(orderKey);
    List<String> args = new ArrayList<>();
    args.add(userId.toString());
    args.add(productId.toString());

    Long result = redisTemplate.execute(script, keys, args.toArray());

    // 4. 处理脚本执行结果
    if (result == -1) {
        throw new RuntimeException("您已经参与过该商品的秒杀");
    } else if (result == 0) {
        throw new RuntimeException("秒杀商品已售罄");
    } else if (result != 1) {
        throw new RuntimeException("秒杀失败，请重试");
    }

    // 5. 异步创建订单
    new Thread(() -> {
        try {
            // 生成订单ID
            Long orderId = generateOrderId();

            // 获取商品信息
            Product product = productMapper.findById(productId);
            if (product == null) {
                // 商品不存在，记录错误
                System.err.println("商品不存在: " + productId);
                return;
            }

            // 创建订单
            Order order = new Order();
            order.setId(orderId);
            order.setUserId(userId);
            order.setProductId(productId);
            order.setQuantity(1); // 秒杀商品每次只能购买1件
            order.setTotalPrice(product.getPrice());
            order.setStatus(1); // 订单状态：1-待支付
            order.setCreateTime(new Date());
            order.setUpdateTime(new Date());

            // 保存订单到数据库
            orderMapper.insert(order);

            // 异步更新数据库库存
            productMapper.decreaseSeckillStock(productId);

            // 发送订单创建消息到Kafka，异步处理
            if (kafkaTemplate != null) {
                try {
                    kafkaTemplate.send("order-create", orderId.toString());
                } catch (Exception e) {
                    // 记录错误日志，但不影响订单创建
                    System.err.println("发送Kafka消息失败: " + e.getMessage());
                }
            }
        } catch (Exception e) {
            // 记录错误日志
            System.err.println("创建秒杀订单失败: " + e.getMessage());
            // 这里可以考虑添加补偿机制，比如将失败的订单信息存入数据库或消息队列
        }
    }).start();

    // 6. 返回临时订单ID
    return System.currentTimeMillis();
}
```

### 5. 雪花算法实现

```java
// 雪花算法生成订单ID
private static final long START_TIMESTAMP = 1609459200000L; // 2021-01-01 00:00:00
private static final long SEQUENCE_BIT = 12; // 序列号占用位数
private static final long MACHINE_BIT = 5;   // 机器标识占用位数
private static final long DATACENTER_BIT = 5; // 数据中心占用位数

private static final long MAX_SEQUENCE = ~(-1L << SEQUENCE_BIT);
private static final long MAX_MACHINE_NUM = ~(-1L << MACHINE_BIT);
private static final long MAX_DATACENTER_NUM = ~(-1L << DATACENTER_BIT);

private static final long MACHINE_LEFT = SEQUENCE_BIT;
private static final long DATACENTER_LEFT = SEQUENCE_BIT + MACHINE_BIT;
private static final long TIMESTAMP_LEFT = DATACENTER_LEFT + DATACENTER_BIT;

private long datacenterId = 1;  // 数据中心ID
private long machineId = 1;     // 机器ID
private long sequence = 0L;      // 序列号
private long lastTimestamp = -1L; // 上次时间戳

// 生成订单ID
private synchronized long generateOrderId() {
    long currentTimestamp = System.currentTimeMillis();

    if (currentTimestamp < lastTimestamp) {
        throw new RuntimeException("Clock moved backwards. Refusing to generate id");
    }

    if (currentTimestamp == lastTimestamp) {
        sequence = (sequence + 1) & MAX_SEQUENCE;
        if (sequence == 0) {
            currentTimestamp = getNextTimestamp(lastTimestamp);
        }
    } else {
        sequence = 0L;
    }

    lastTimestamp = currentTimestamp;

    return ((currentTimestamp - START_TIMESTAMP) << TIMESTAMP_LEFT)
            | (datacenterId << DATACENTER_LEFT)
            | (machineId << MACHINE_LEFT)
            | sequence;
}
```

## 项目运行

### 1. 环境准备

- Docker已安装并运行
- Maven已安装

### 2. 启动服务

1. 在项目目录下执行：
   ```
   docker-compose up -d
   ```

2. 服务启动后，访问：
   - 项目首页：http://localhost
   - API接口：http://localhost/api

### 3. 测试接口

- **用户注册**：POST http://localhost/api/auth/register
  - 请求体：{"username": "test", "password": "123456", "email": "test@example.com"}

- **用户登录**：POST http://localhost/api/auth/login
  - 请求体：{"username": "test", "password": "123456"}

- **获取商品详情**：GET http://localhost/api/products/1

- **创建订单**：POST http://localhost/api/orders?userId=1&productId=1&quantity=1

- **搜索商品**：GET http://localhost/api/search?keyword=商品

## 总结

本项目实现了分布式系统的核心功能，包括：

1. **分布式缓存**：使用Redis实现商品详情页缓存，处理了缓存穿透、击穿、雪崩问题
2. **读写分离**：配置了MySQL的读写分离，通过AOP切面自动切换数据源
3. **负载均衡**：使用Nginx实现后端服务的负载均衡
4. **动静分离**：通过Nginx实现静态资源和动态请求的分离处理
5. **消息队列**：使用Kafka实现订单的异步处理
6. **搜索引擎**：集成ElasticSearch实现商品搜索功能
7. **秒杀功能**：实现了基于Redis和Kafka的秒杀系统

项目采用了微服务架构，通过Docker容器化部署，具有良好的可扩展性和可维护性。