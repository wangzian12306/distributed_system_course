# 分布式软件原理与技术 - 核心知识整理
## 一、分布式软件基础定义
### 1. 分布式软件定义
分布式软件是指组件分布在通过网络连接的不同计算机（节点）上，通过传递消息通信协调，共同完成一个任务的软件系统。
### 2. 核心本质
- **物理上的分散**：软硬件组件位于网络连接的不同计算机/虚拟机/容器中
- **消息传递通信**：节点无共享内存，通过网络消息交互
- **共同目标**：所有节点为完成同一任务协同工作（如支撑双十一流量）
### 3. 分布式的核心目标（三高）
- **高并发**：多机水平扩展支撑海量同时请求（如100台机器支撑5万并发）
- **高性能**：任务拆分并行处理，大幅缩短执行时间（如10台机器将10天任务缩至1天）
- **高可用**：部分节点故障不影响整体服务（如10台服务器坏1台，其余9台继续工作）
### 4. 分布式的副作用
分布式引入极高复杂度，核心问题为：
- **可靠性**：网络分区、消息丢失/延迟/乱序
- **一致性**：多节点数据更新不同步，导致用户看到错误数据
> 核心原则：分布式为解决单机规模化和容错性问题，不可为了分布式而分布式，否则系统会复杂且脆弱。

### 5. 分布式相关术语辨析
|术语|范畴|定义|解决的关键问题|
| ---- | ---- | ---- | ---- |
|分布式计算|计算范式|将巨量计算任务拆分为子任务，多机同时处理并汇总结果|任务切分、并行处理等计算优化|
|分布式系统|软硬件集合体|软硬件组件分布在不同网络计算机，仅通过消息传递通信协调|节点、网络、局部失效、逻辑时钟等|
|分布式架构|系统设计|分布式系统内部组件的组织与交互的结构化设计方案|拓扑结构、通信协议解耦、服务治理等|
|分布式软件|软件产品|运行在分布式系统上的应用软件，由多协同组件组成|高并发、高性能、高可用、高可靠（一致性）|

## 二、分布式软件发展史
分布式架构经历六个发展阶段，各阶段核心特征与技术如下：
### 1. 集中式时代（1960s-1980s）
- 核心特征：大型机统治，代表为IBM System/360
- 特点：单台大型机承载所有计算与存储，无分布式概念
### 2. 分布式萌芽（1970s末-1980s）
- 时代背景：微型计算机兴起（Intel 8086，1978），单机算力不足催生多机协作
- 核心技术：RPC雏形（NCA）、AFS分布式文件系统、Kerberos认证协议，里程碑为DCE分布式运算环境
- 历史教训：“透明分布式调用”理想超前，网络问题导致复杂度远超收益
### 3. 分布式组件时代（1990s-2000s）
- 核心技术：1992年OMG组织推出CORBA，试图实现跨语言、跨平台的分布式对象统一
- 失败原因：违背简单原则，学习成本高、厂商不兼容，败给轻量Web技术
### 4. 单体时代（1990s-2010s）
- 背景：互联网应用崛起、摩尔定律硬件红利
- 核心技术：简单高效的单体架构，**单体≠单机，也可成为分布式软件**
- 架构演进路径：单应用单数据库→应用与数据库分离→应用服务器集群（负载均衡）→数据库读写分离→引入缓存（Redis/Memcached）→数据库垂直/水平拆分
### 5. SOA时代（2000s-2010s）
- 背景：异构应用集成需求
- 核心技术：ESB企业服务总线，SOAP协议交互
- 缺点：ESB成为性能瓶颈和单点故障；SOAP协议笨重，向RESTful轻量协议转型
### 6. 微服务时代（2010s-2020s）
- 背景：应用规模扩大，单体应用开发运维效率低下
- 核心理念：将单体拆分为独立部署小服务，各服务拥有独立数据库，通过轻量级API通信
- 优点：服务由独立团队开发测试，解决分工和开发效率问题
- 缺点：增加分布式事务、链路追踪等开发和运维复杂性
- 服务治理：容器化成为标准化部署方式，Kubernetes实现自动化治理，与Spring Cloud核心能力对比如下：
|能力|Kubernetes|Spring Cloud|
| ---- | ---- | ---- |
|弹性伸缩|Autoscaling|N/A|
|服务发现|KubeDNS/CoreDNS|Eureka|
|配置中心|ConfigMap/Secret|Config|
|服务网关|Ingress Controller|Zuul|
|负载均衡|Load Balancer|Ribbon|
|服务安全|RBAC API|Security|
|跟踪监控|Metrics API/Dashboard|Turbine|
|降级熔断|N/A|Hystrix|
### 7. 云原生时代（2020s至今）
- 核心技术1：Service Mesh服务网格，服务治理下沉至基础设施（Sidecar模式），对开发者透明
- 核心技术2：BaaS后端即服务，云上直接提供数据库、消息队列等后端服务
- 核心技术3：Serverless无服务器架构，提供FaaS函数即服务，支持scale to zero，节省运营成本

### 8. 典型案例：淘宝架构演进之路
淘宝架构随业务增长逐步升级，核心步骤为：
1. 单机架构→2. Tomcat与数据库分开部署→3. 引入缓存→4. 引入反向代理与负载均衡/LVS/F5多路负载均衡→5. 数据库读写分离→6. 按业务分库→7. 数据库分表→8. DNS层负载均衡→9. 引入NoSQL数据库和搜索引擎→10. 应用分解→11. 引入微服务架构→12. 引入服务总线→13. 引入容器与K8S

## 三、分布式核心理论
### 1. CAP定理
CAP定理是分布式系统的基础理论，核心为**分布式系统中无法同时完全满足一致性（C）、可用性（A）、分区容错性（P）**。
#### （1）三个核心概念
- **一致性（C）**：所有节点在同一时间看到相同数据（强一致性）
- **可用性（A）**：系统随时可响应请求，即使有节点故障
- **分区容错性（P）**：网络中断或节点失联时，系统仍能运作
#### （2）核心权衡逻辑
网络分区是分布式系统的客观存在，必须保障P，因此只能在A和C之间二选一：
- **CP（牺牲A）**：为保证一致性，网络分区时部分节点拒绝请求；适用场景：银行系统、金融交易、分布式锁（ZooKeeper、HBase）
- **AP（牺牲C）**：为保证可用性，网络分区时各节点继续处理请求，允许数据暂时不一致；适用场景：社交媒体点赞、购物车、新闻推送（Redis Cluster、Cassandra）
- **CA（牺牲P）**：仅适用于单机系统，如单机数据库（ACID）

### 2. PACELC定理
PACELC是对CAP的补充，**涵盖故障（分区）和正常运行两种状态的抉择**，公式为：P-A-C / E-L-C。
#### （1）公式拆解
- **P-A-C**：发生网络分区时，在可用性（A）和一致性（C）之间权衡（与CAP一致）
- **E-L-C**：系统正常运行时，在延迟（L）和一致性（C）之间权衡
#### （2）四大策略类型
|策略类型|典型代表|核心特点|
| ---- | ---- | ---- |
|PA/EL|Dynamo、Cassandra|分区选可用；正常时选低延迟（牺牲强一致性）|
|PC/EC|BigTable、HBase|始终追求强一致性；分区不可用、正常时高延迟|
|PC/EL|MongoDB（默认）|分区选一致性；正常时为性能选低延迟|
|PA/EC|VoltDB|分区保可用；正常时维持强一致（开销大）|
#### （3）核心要点
- CAP处理故障场景，PACELC覆盖日常运行场景，二者结合构成完整设计空间
- 延迟是分布式系统第一性能指标，强一致性往往意味着高延迟
- 可调一致性是现代分布式平台标配，可根据业务场景选择一致性级别

### 3. BASE理论（AP系统实践指南）
BASE是分布式系统的**软一致性模型**，是CAP中选择AP后的具体设计原则，与传统ACID强一致性模型形成对比。
#### （1）BASE核心概念
- **基本可用（Basically Available）**：系统大部分时间对核心功能可用，部分节点故障时牺牲非核心功能响应速度
- **软状态（Soft State）**：允许系统在不保证强一致性的情况下，存在短暂的数据不一致（数据版本不立即同步，状态变化无需ACID特性）
- **最终一致性（Eventual Consistency）**：即使中间状态不一致，经过一段时间后，所有节点数据终将达到一致（无新更新时，所有请求返回相同值）
#### （2）BASE核心思想
1. 接受短暂数据不一致，换取系统高可用和高性能
2. 最终一致性不是牺牲一致性，而是有保障的一致性，需保证收敛时间在业务可接受范围内
3. 无银弹：强一致性系统简单正确但慢，最终一致性系统复杂但快

#### （3）BASE与ACID系统对比
|维度|ACID系统（CP典型实现）|BASE系统（AP典型实现）|
| ---- | ---- | ---- |
|一致性|强一致性|最终一致性|
|可用性|低（维护一致性可能不可用）|高（基本可用）|
|事务|支持复杂事务|简单事务或补偿事务|
|响应时间|较慢|较快|
|设计复杂度|相对简单|相对复杂（需处理数据不一致）|
|典型系统|MySQL、PostgreSQL|Cassandra、Dynamo、Redis|
|适用场景|转账、支付、库存扣减|社交、推荐、购物车、会话数据|

### 4. 分布式理论常见面试问题
|问题|核心答案|
| ---- | ---- |
|CAP和PACELC什么关系？|PACELC是CAP的扩展，增加了正常运行时延迟与一致性的权衡|
|为什么有些系统正常运行时也选弱一致性？|为降低延迟，强一致需同步复制，增加响应时间，互联网场景中延迟影响用户体验和转化率|
|CAP和BASE有什么关系？|CAP是理论边界，BASE是AP系统的实践指南，选择AP时用BASE指导设计|
|什么场景不能用最终一致性？|金融交易核心链路（扣款）、医疗系统关键操作、法律文件签署等需强一致性的场景|

## 四、分布式软件的关键问题
分布式软件的核心目标是实现**高并发、高性能、高可用**，基础要求为**高可靠、一致性**，各属性相互依赖、彼此权衡。

### 1. 五大核心属性定义与指标
|概念|核心定义|关键指标|通俗理解|解决方案|
| ---- | ---- | ---- | ---- | ---- |
|**高并发**|系统同时处理大量请求的能力|QPS（每秒查询数）、TPS（每秒事务数）、并发连接数|“扛得住”|横向扩展（负载均衡）、缓存策略（Redis）、异步处理（MQ削峰）|
|**高性能**|单个请求的处理速度与资源利用效率|响应时间（RT）、延迟（p99）、CPU/内存/I/O利用率|“跑得快”|算法优化、代码优化（异步IO、细粒度锁）、硬件加持（SSD、大带宽）|
|**高可用**|组件故障时系统仍能持续提供服务的能力|SLA（如99.99%=年停机52分钟）、MTBF（平均无故障时间）、MTTR（平均恢复时间）|“不宕机”|多机房容灾、故障自动切换、服务降级/熔断|
|**高可靠**|系统正确完成任务，数据不丢失、计算结果正确|持久性、错误率|“不出错”|故障隔离、故障转移、容错与共识（Raft/Paxos）、可观测性（监控/告警）|
|**一致性**|多数据副本之间的数值关系|一致性模型（强/弱/最终等）|“不矛盾”|强一致：2PC/3PC/Raft；最终一致：Gossip/Quorum；分布式事务：TCC/Saga|

### 2. 一致性模型细分
一致性是分布式软件的核心难点，分为**强一致性**、**弱一致性**两大类别，包含多种细分模型，各模型适用不同业务场景：
#### （1）强一致性
- **线性一致性**：操作原子、瞬时生效，所有节点在真实时间看到相同顺序，是最强一致性；适用：分布式锁（etcd/ZK）、支付宝余额更新
- **顺序一致性**：所有进程看到的操作顺序一致，与真实时间无关，仅保留单个进程内操作顺序；适用：消息队列、HDFS文件元数据操作

#### （2）弱一致性
弱一致性放弃全局强一致，在局部或时间维度做一致性保障，包含6类细分模型：
|模型|核心规则|适用场景|
| ---- | ---- | ---- |
|因果一致性|有因果关系的操作按顺序执行，无因果关系的并发操作顺序任意|微信聊天、腾讯文档协同编辑、社交评论回复|
|读写一致性|用户能读到自己刚写入的最新值，不保证跨用户一致|修改头像昵称、购物车、点赞后立即显示|
|会话一致性|同一会话内满足读写一致性，不同会话间无保证|Web购物下单、在线游戏状态|
|单调读一致性|用户不会读到更旧的数据，避免“时光倒流”|朋友圈、微博时间线|
|单调写一致性|同一用户的写操作按提交顺序执行，后发不先至|积分增减、等级提升|
|最终一致性|无新更新时，所有副本终将收敛到相同值|DNS解析、普通商品库存、微博粉丝数/播放量|

### 3. 各属性的依赖关系
分布式软件的五大核心属性并非独立，上层属性依赖下层属性支撑，核心依赖关系如下：
|上层属性|依赖的下层属性|依赖原因|
| ---- | ---- | ---- |
|高并发|高性能、高可用|单请求处理慢则无法支撑海量并发；服务不可用则并发数为0|
|高性能|高可靠、一致性|数据不一致会导致重试/错误处理，拖慢请求响应|
|高可用|高可靠、一致性|故障恢复时需要从一致的可靠数据副本重建服务|
|高可靠|一致性|多副本数据不一致，则数据可靠性无从谈起|

## 五、实践作业：商品库存与秒杀系统设计
### 1. 系统设计文档要求
1. 绘制系统架构草图：拆分用户服务、商品服务、订单服务、库存服务
2. 定义各服务API接口：遵循RESTful规范
3. 设计数据库ER图：包含用户表、商品表、库存表、订单表
4. 技术栈选型说明：确定编程语言、框架、中间件初选方案
### 2. 环境准备要求
1. 初始化项目代码仓库（Git）
2. 搭建基础开发环境（如Spring Boot + MyBatis + MySQL）
3. 搭建项目代码框架，实现简单的用户注册、登录功能


二、# 分布式软件原理与技术 - 高并发设计（1）核心知识整理
## 一、高并发基础认知
### 1. 核心定义
高并发是分布式软件的核心目标与挑战之一，指系统**同时处理大量并发请求的能力**，核心衡量指标为吞吐量（QPS/TPS）、并发用户数，通俗理解为系统“扛得住”的流量规模（如双十一零点百万级请求）。
**关键注意**：高并发≠高吞吐，需同时关注响应时间，避免“慢吞吞处理大量请求”。

### 2. 并发评估核心指标
|指标|全称|定义|典型适用场景|
| ---- | ---- | ---- | ---- |
|QPS|Queries Per Second|每秒查询数|搜索引擎、商品列表查询等**读场景**|
|TPS|Transactions Per Second|每秒事务数|下单、支付、扣费等**写场景**|
|并发用户数|Concurrent Users|同一时刻活跃操作的用户数|在线聊天室、直播互动|

### 3. 与其他系统属性的关系
- **依赖高性能**：单请求处理慢，并发能力必然受限；
- **依赖高可用**：服务不可用则并发数为0；
- **与高可靠/一致性权衡**：强一致、高可靠需同步复制、分布式事务，会增加延迟、降低并发；
- **典型案例**：秒杀系统通过**异步削峰+最终一致性**保障高并发，牺牲强一致性。

## 二、高并发系统分类及解决方案
根据读写操作占比，高并发系统分为**高并发读**、**高并发写**、**高并发读写混合**三类，各类系统核心特征、挑战与技术方案差异显著。

### 1. 高并发读系统
#### 核心特征
读操作占绝对主导，**读写存在巨大鸿沟**：
1. 请求规模：C端读请求达数亿/数十亿级，写请求仅百万/千万级；
2. 响应时间：读要求**毫秒级**（超100ms用户感知延迟，超1s流失率飙升），写可容忍分钟/小时级延迟；
3. 操作频率：读为高频（分钟级刷新），写为低频（天/周级更新）。
#### 典型场景
搜索引擎（百度/Google）、电商商品搜索/详情页、新闻资讯展示。
#### 核心挑战
- 流量海啸：大促/热点事件流量飙升至日常数百倍；
- 响应极度敏感：99.9%请求需100ms内响应，延迟1s流失率提升10%+；
- 数据复杂度高：单请求需聚合多数据源数十个字段；
- 可用性要求苛刻：交易链路起点，故障直接导致订单流失。
#### 技术方案：分层防御与流量治理
核心思路：**分而治之+多层缓冲**，让请求在离用户最近处处理，避免穿透后端数据库，采用四层架构：
|层级|核心目标|关键技术|优势|核心挑战|
| ---- | ---- | ---- | ---- | ---- |
|接入层|过滤、分发、隔离流量|CDN加速、SLB负载均衡、API网关|过滤无效流量，分发有效流量|过滤恶意爬虫流量|
|缓存层|降低数据库压力，提升响应速度|本地缓存、分布式缓存、数据库缓存|快速承接高频热点请求|缓存数据一致性|
|服务层|解耦与弹性伸缩|服务拆分、RPC调用、弹性伸缩|专岗专责，弹性扩展|微服务间高效调用|
|数据层|高可用、一致性、实时性|分库分表、读写分离、数据一致性|避免单库单表压力过大|数据同步延迟|

### 2. 高并发写系统
#### 核心定义
写操作占主导，极端流量下需保证数据**准确、一致、持久化**，是企业“资金流/业务核心”，对一致性要求严苛。
#### 典型场景
广告计费系统（CPM/CPC实时扣费）、微博/朋友圈内容发布、金融交易记账。
#### 核心挑战
- 流量洪峰：高峰TPS可达10万+（如广告计费系统）；
- 热点行并发：热点字段（如广告主余额）引发锁竞争、I/O瓶颈；
- 强一致性+高性能矛盾：传统数据库同步提交成为并发瓶颈；
- 数据可追溯性：写操作需可审计、可对账、支持异常补偿。
#### 技术方案：四层架构层层拆解
|层级|核心策略|关键技术|本层意义|
| ---- | ---- | ---- | ---- |
|入口层|流量管控|限流、熔断、降级|系统第一道防线，仅让可处理流量进入|
|缓冲层|削峰填谷|消息队列（Kafka/RocketMQ）、异步化|同步转异步，拉平瞬时高峰，提供稳定数据流|
|处理层|聚合与分散|数据分片、批量处理、缓存写（Write-Behind）|加工数据，减少操作、分散压力|
|存储层|数据落地保障|数据库集群、锁优化、幂等设计、读写分离|保证数据正确、一致、可靠持久化|

### 3. 高并发读写混合系统
#### 核心定义
读写操作均面临巨大并发压力，要求**数据实时性、一致性+系统稳定性**，处于业务核心链路，对用户体验和资金安全要求极高。
#### 典型场景
12306售票系统、电商秒杀系统、支付/微信红包系统、IM/评论系统。
#### 核心挑战
- 热点数据争用：爆款SKU库存、春晚红包等热点数据成为节点瓶颈；
- 读写互相干扰：写操作占满磁盘I/O，导致读延迟飙升；
- 强一致性与高性能平衡：锁/分布式事务增加延迟，无锁易导致数据不一致；
- 系统稳定性与可扩展性：流量洪峰下保持稳定，支持水平扩展。
#### 技术方案：读写分离+分层协同
核心思路：从入口分离读写流量，通过缓冲、多级缓存、数据分片实现读写协同，保障一致性与性能：
1. **入口层**：通用流量管控（限流、熔断、降级）；
2. **路由层**：读写识别与分发，读请求走缓存/从库，写请求走主库/消息队列；
3. **缓冲层（写）**：异步化削峰，写请求先入MQ，立即返回“已接收”；
4. **处理层**：读用多级缓存（本地+分布式），写用批量聚合，通过Cache Aside保证缓存与数据库一致性；
5. **存储层**：数据分片、主从复制、锁优化（乐观锁）、幂等设计，通过MQ/定时任务实现最终一致性补偿。

## 三、接入层优化：动静分离与CDN加速
### 1. 动静分离
#### 核心原因
静态资源与动态内容的**访问路径、资源消耗、缓存友好性**差异显著，分开处理可大幅提升性能、降低后端负载。
#### 静态/动态资源区分
|类型|定义|典型示例|核心特征|
| ---- | ---- | ---- | ---- |
|静态资源|几乎不变/变化频率极低的文件|图片、CSS、JS、视频、字体、HTML|访问路径短、消耗I/O/带宽、可多级缓存|
|动态内容|服务器代码实时生成的内容|用户信息、实时库存、搜索结果|需业务计算/数据库查询、消耗CPU/数据库连接、难以缓存|
#### 定义
将Web应用中的静态资源与动态内容分开处理的设计方法，**前端代码均属于静态资源**。
#### 核心实现：Nginx动静分离
Nginx基于epoll异步非阻塞模型，处理静态文件效率极高，是动静分离的核心组件。
##### 核心优势
- 降低应用服务器负载：静态请求由Nginx直接处理，不占用后端资源；
- 提升响应速度：Nginx静态文件处理性能远高于应用服务器；
- 缓存策略灵活：可设置强缓存（expires），减少重复请求。
##### 配置示例
```nginx
server {
    listen 80;
    server_name example.com;
    # 静态资源处理：匹配目录/后缀
    location /static/ {
        alias /var/www/static/;
        expires 30d; # 浏览器缓存30天
        access_log off; # 关闭静态资源日志
    }
    location ~* \.(jpg|jpeg|png|gif|css|js|ico)$ {
        root /var/www/static;
        expires 30d;
        access_log off;
    }
    # 动态请求转发至后端应用集群
    location / {
        proxy_pass http://backend_servers;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```
##### 部署原则
前端代码/文件直接通过Nginx访问，后端接口请求由Nginx转发至应用服务器端口。

### 2. CDN加速
#### 核心原理：就近访问
通过在全国/全球部署边缘节点，将静态资源缓存至边缘节点，用户请求优先访问最近的边缘节点，避免直接访问源站，降低延迟、节省带宽。
#### 两类CDN
|类型|加速范围|核心逻辑|
| ---- | ---- | ---- |
|传统CDN|仅静态资源|静态资源缓存至边缘节点，节点无资源则回源站拉取|
|全站加速CDN|静态+动态内容|不缓存动态内容，优化请求路径（如专线传输），提升动态访问速度|
#### 工作流程
1. 用户发起域名请求，经本地DNS（LDNS）解析；
2. LDNS请求网站授权DNS，返回CNAME指向CDN调度系统；
3. CDN调度系统根据用户位置，返回最近的边缘节点IP；
4. 用户请求边缘节点，节点有缓存则直接响应，无则回源站拉取并缓存；
5. 边缘节点将内容返回给用户。

## 四、负载均衡
### 1. 核心定义
多台服务器组成对称集群，通过负载分担技术将外部请求**均匀分配**到集群中的服务器，单台服务器独立响应请求。
**核心公式**：负载均衡 = 统一入口 + 二次分发。
#### 核心价值
|业务需求|原有问题|负载均衡解决方案|
| ---- | ---- | ---- |
|高性能|单台服务器处理能力有限|多机集群，总处理能力线性扩展|
|高可用|单点故障导致服务中断|多机互为备份，故障自动切换|
|高扩展|业务增长需扩容|随时添加服务器，对外部透明|

### 2. 负载均衡分类
#### 按部署方式分
|类型|代表产品|核心特点|适用场景|
| ---- | ---- | ---- | ---- |
|软件负载均衡|LVS、Nginx、HAProxy、Traefik|通用服务器、灵活扩展、成本低|互联网公司、云原生环境|
|硬件负载均衡|F5 BIG-IP、A10|专用硬件、吞吐量大、成本高|金融核心系统、大型数据中心|
#### 按OSI层级分（最常用分类）
|层级|代表产品|工作原理|核心优势|适用场景|
| ---- | ---- | ---- | ---- |
|四层（传输层）|LVS、F5、阿里云SLB|基于**IP+端口**转发，仅修改报文目标/源IP，不解析内容|性能极高、开销小，三次握手由客户端与后端直接建立|数据库、TCP/UDP服务、高吞吐场景|
|七层（应用层）|Nginx、HAProxy、Tengine|基于**反向代理**，解析HTTP/HTTPS报文内容（URL/Cookie/Header）转发|内容感知、智能路由，支持SSL卸载、会话保持|Web应用、API网关、微服务流量入口|
#### 最优组合：四层+七层
**四层在前，七层在后**：四层抗流量洪峰，做高吞吐基础转发；七层做精细化分发，实现内容路由、SSL卸载等高级功能。

### 3. 负载均衡算法
分为**静态算法**和**动态算法**，算法选择直接影响系统吞吐量、响应时间和稳定性。
#### 算法分类及核心特征
|类型|核心定义|特点|典型算法|
| ---- | ---- | ---- | ---- |
|静态算法|不考虑服务器实时状态，按固定规则分配|实现简单、开销小；无法应对负载变化|轮询、加权轮询、IP哈希、随机|
|动态算法|根据服务器实时负载（连接数、响应时间等）动态分配|负载分配更均衡；需实时采集数据，开销稍大|最少连接、加权最少连接、响应时间、最快响应|

#### 静态算法详情
|算法|原理|特点|
| ---- | ---- | ---- |
|轮询|请求依次循环分配给集群服务器|完全平均，实现简单；忽略服务器负载差异|
|加权轮询|按预设权重分配请求，权重高则请求多|适配服务器性能异构场景；权重需人工配置|
|IP哈希|对客户端IP哈希计算，映射到固定服务器|实现会话保持；可能因IP分布不均导致负载失衡|
|随机|从服务器列表随机选择一个|大流量下趋于平均，实现极简|

#### 动态算法详情
|算法|核心原理|适用场景|
| ---- | ---- | ---- |
|最少连接|新请求分配给**当前活跃连接数最少**的服务器|长连接服务（FTP、WebSocket、数据库连接池）|
|加权最少连接|计算`连接数/权重`，选择比值最小的服务器|服务器性能不同且连接时长差异大的场景|
|响应时间|选择**最近探测响应时间最短**的服务器|延迟敏感的在线服务（API网关、搜索）|
|最快响应|选择**历史平均响应时间最快**的服务器|响应时间波动较大的环境|
|预测算法|根据历史负载趋势预测未来负载，选择预测最低的|负载有明显周期性的业务（定时促销、夜间任务）|

#### 场景化算法选择
|业务场景|推荐算法|选择原因|
| ---- | ---- | ---- |
|静态网站（短连接、请求快）|轮询/加权轮询|请求处理时间相近，简单公平即可|
|API服务（响应时间敏感）|响应时间/最少连接|优先保证低延迟，避免慢服务器拖累整体|
|WebSocket/IM（长连接）|最少连接|避免连接集中在某几台服务器|
|需要会话保持（登录态）|IP哈希/Cookie插入（七层）|确保用户请求始终由同一服务器处理|
|服务器性能异构集群|加权轮询/加权最少连接|充分发挥高性能服务器的处理能力|
|下载服务/大文件传输|最少连接|避免单台服务器因大流量过载|
|数据库负载均衡|最少连接/响应时间|数据库连接长时间占用，需动态调整|

#### 算法组合配置示例（Nginx）
不同请求路径配置不同算法，兼顾性能、会话保持和简单性：
```nginx
# API服务：最少连接（保障响应速度）
upstream api_servers {
    least_conn;
    server 192.168.1.10:8080;
    server 192.168.1.11:8080;
}
# 静态资源：轮询（简单快速）
upstream static_servers {
    round_robin;
    server 192.168.1.20:80;
    server 192.168.1.21:80;
}
# 用户中心：IP哈希（保持登录会话）
upstream user_servers {
    ip_hash;
    server 192.168.1.30:8080;
    server 192.168.1.31:8080;
}
# 路径转发
server {
    listen 80;
    server_name example.com;
    location /api/ { proxy_pass http://api_servers; }
    location /static/ { proxy_pass http://static_servers; expires 30d; }
    location /user/ { proxy_pass http://user_servers; proxy_set_header X-Real-IP $remote_addr; }
}
```

### 4. 负载均衡关键能力
#### 会话保持
##### 定义
将同一客户端的请求始终分发到同一台后端服务器，保证登录信息、购物车等**会话状态不丢失**。
##### 主流实现方式
|方式|实现原理|适用层级|优缺点|
| ---- | ---- | ---- | ---- |
|源地址哈希|对客户端IP哈希，映射到固定服务器|四层|实现简单；IP分布不均易导致负载失衡|
|Cookie插入|七层负载均衡生成Cookie，客户端后续请求携带|七层|负载均衡；依赖客户端开启Cookie|
|Redis集中存储会话|服务器无状态，会话存储在共享Redis中|四层/七层|无负载失衡问题；需维护Redis集群|
|无状态Token（JWT）|客户端存储Token，包含用户信息+签名，服务器验证即可|四层/七层|无需存储会话，易扩展；主流推荐方案|

#### 一致性哈希
##### 核心原理
将请求Key（IP/URL）和服务器均哈希到**2^32位哈希环**上，请求Key落在最近的服务器节点上，服务器节点映射为**虚拟节点**（解决负载不均）。
##### 核心优点
支持节点**动态扩缩容**，服务器增减时，仅少量请求需要重新分配，最小化数据迁移。
##### 适用场景
分布式缓存（Redis分片）、CDN边缘节点、分布式存储。

## 五、缓存：高并发读的核心优化手段
### 1. 缓存基础认知
#### 定义
将数据存储在**临时高速存储介质（主要是内存）** 中，加速后续数据访问的技术。
#### 核心价值
- 降低延迟：从磁盘级（10-50ms）降至内存级（0.1-1ms）；
- 减轻数据库压力：拦截90%以上的读请求；
- 提升系统吞吐：用内存的高吞吐换取系统并发能力。
#### 直接访问DB vs 访问缓存
|对比维度|直接访问数据库|访问缓存|
| ---- | ---- | ---- |
|存储介质|磁盘（机械/SSD）|内存（DRAM）|
|访问延迟|10-50ms（SSD）或更高|0.1-1ms|
|吞吐能力|几百~几千 QPS|数万~数十万 QPS|
|成本|相对低廉|较高|

### 2. 本地缓存 vs 分布式缓存
|维度|本地缓存|分布式缓存|
| ---- | ---- | ---- |
|代表产品|Caffeine、Guava Cache、Ehcache|Redis、Memcached|
|存储位置|应用进程内内存|独立服务器内存|
|访问速度|极快（纳秒级，无网络开销）|快（毫秒级，有网络开销）|
|数据一致性|各节点独立，需自行同步|所有应用共享同一份数据|
|适用场景|静态配置、热点数据二级缓存|全局共享数据、会话存储、核心业务缓存|

### 3. 分布式缓存核心：Redis
#### 性能表现
单节点读写QPS可达10万+，平均响应时间<1ms，阿里双11峰值调用量达5亿次/秒。
#### 高并发核心技术特性
|技术特性|说明|对高并发的贡献|
| ---- | ---- | ---- |
|纯内存存储|数据全部在内存中操作|避免磁盘I/O瓶颈，提升吞吐|
|单线程+多路复用|基于epoll/Reactor模型，无锁竞争|上下文切换开销小，高并发下稳定|
|高效数据结构|支持String、Hash、List、Set、ZSet等|多数操作O(1)，适配不同业务场景|
|原子操作|所有指令都是原子的|天然支持高并发计数、限流、扣减|

### 4. 缓存核心读写模式
主流有三种缓存读写模式，适用于不同业务场景，核心差异在**一致性、性能、复杂度**。
#### （1）Cache Aside（旁路缓存模式）- 最常用
##### 核心流程
- **读**：先查缓存，命中直接返回；未命中查数据库，将数据写入缓存后返回；
- **写**：**先更新数据库，再删除缓存**（避免并发查询回写旧数据），而非“先删缓存再更DB”。
##### 异常处理：删除缓存失败
- 策略1：重试机制 + 缓存过期时间兜底；
- 策略2：监听数据库binlog（Canal），独立服务后台删除缓存。
##### 核心特点
强最终一致、性能高、实现简单，是电商商品详情、用户资料等场景的首选。

#### （2）Read/Write Through（缓存代理模式）
##### 核心流程
- **读**：缓存未命中时，**由缓存自行加载数据库数据**，应用无需关心加载逻辑；
- **写**：应用仅更新缓存，**由缓存同步更新数据库**，保证缓存与DB强一致。
##### 核心特点
强一致性（同步）、性能中等、实现复杂度中等，适用于配置管理中心、游戏玩家状态。

#### （3）Write Behind（异步写回模式）
##### 核心流程
- 应用先更新缓存，缓存**异步批量写入数据库**，可合并多次写操作。
##### 核心特点
- 优点：性能极高（仅操作内存）、削峰填谷；
- 缺点：数据可能丢失（缓存宕机）、最终一致性（DB数据有延迟）；
##### 适用场景
点赞计数、访问统计、日志收集、购物车合并保存（允许短暂不一致的场景）。

#### 三种模式对比
|模式|读流程|写流程|一致性|性能|复杂度|场景示例|
| ---- | ---- | ---- | ---- | ---- | ---- | ---- |
|Cache Aside|应用加载DB数据至缓存|先更DB，再删缓存|强最终一致|高|低|电商商品详情、用户资料|
|Read/Write Through|缓存代理加载DB数据|缓存代理同步更DB|强一致（同步）|中|中|配置管理中心、游戏玩家状态|
|Write Behind|同Read Through|缓存异步批量更DB|最终一致|极高|高|点赞计数、访问统计、购物车|

### 5. 高并发下的缓存三大问题及解决方案
缓存穿透、缓存击穿、缓存雪崩是高并发场景下的核心缓存问题，均表现为**缓存未命中，请求穿透到数据库**，但成因和解决方案差异显著。

|问题|核心成因|典型场景|核心危害|
| ---- | ---- | ---- | ---- |
|缓存穿透|请求**不存在的数据**，缓存和DB均无|恶意攻击（id=-1）、爬虫扫描、业务数据未生成|数据库压力激增，甚至宕机|
|缓存击穿|**单个热点Key**在失效瞬间，大量请求同时打穿缓存|秒杀商品信息失效、微博热搜更新、爆款详情页|单热点Key导致数据库崩溃|
|缓存雪崩|**大量Key同时过期**或Redis集群整体故障|所有Key设相同过期时间、系统重启缓存清空、Redis宕机|请求全部打穿到DB，整个系统瘫痪|

#### （1）缓存穿透解决方案
|方案|核心原理|优点|缺点|
| ---- | ---- | ---- | ---- |
|缓存空值|查询不到的数据也缓存，设置**短过期时间**（如5分钟）|实现简单，效果直接|可能缓存大量空Key，占用内存|
|布隆过滤器|预加载所有合法Key，请求前先过滤，非法Key直接拦截|内存占用极小，拦截彻底|实现复杂，存在**误判率**（判定存在的Key可能实际不存在）|
|参数校验|在入口层校验请求参数合法性（如id>0）|简单快速，低开销|仅能拦截明显非法请求，无法应对隐蔽攻击|

#### （2）缓存击穿解决方案
核心思路：避免大量请求同时穿透到数据库，让**单个线程/进程负责加载DB数据更新缓存**。
|方案|核心原理|一致性|性能|复杂度|适用场景|
| ---- | ---- | ---- | ---- | ---- | ---- |
|分布式锁|缓存失效时，只有一个线程能获取锁并查DB更新缓存，其他线程等待重试|强最终一致|有锁开销（轻微）|中|对一致性要求稍高的场景|
|逻辑永不过期|缓存不设置**物理过期时间**，仅设逻辑过期时间；过期时返回旧数据，**异步刷新缓存**|最终一致（短暂延迟）|极高（无锁）|较高|允许短暂不一致，追求极致性能的场景|
##### 分布式锁关键注意事项
- 锁粒度：与具体热点Key绑定（如`lock:product:1001`），避免全局锁；
- 锁超时：必须设置自动过期时间（如5秒），防止线程崩溃导致死锁；
- 锁重试：未获锁线程不可无限等待，设置重试次数/最大等待时间；
- 锁删除：仅持有锁的线程可删除，避免误删他人锁。

#### （3）缓存雪崩解决方案
核心思路：**避免大量Key同时失效**+**Redis高可用**+**多层缓存兜底**+**数据库限流保护**，多策略组合防御：
|策略|核心实现|效果|
| ---- | ---- | ---- |
|过期时间加随机值|基础过期时间 + 随机值（如3600s + 0-300s）|避免所有Key集体失效，基础策略|
|缓存预热|系统启动时，预先加载热点数据至缓存|避免冷启动空窗期，缓存为空导致穿透|
|多级缓存|本地缓存（L1）+ 分布式缓存（L2）|即使Redis宕机，本地缓存可保底，拦截部分请求|
|限流熔断|在数据库前加限流（如Sentinel），超出阈值则熔断|防止数据库被海量请求冲垮|
|Redis高可用|主从+Sentinel/Redis Cluster部署|避免Redis单点故障，提升集群可用性|
##### 缓存预热核心步骤
离线分析（历史日志找热点）→ 分级加载（核心数据全量加载）→ 流量控制（避免预热压垮Redis）→ 进度监控（监控预热完成率）。
##### 多级缓存架构（L1+L2+L3）
|层级|存储介质|访问时间|容量|适用数据|
| ---- | ---- | ---- | ---- | ---- |
|L1 本地缓存|应用内存|纳秒级|小（MB级）|超热点数据、静态配置|
|L2 分布式缓存|Redis内存|1-3ms|大（GB/TB级）|全局共享数据、会话数据|
|L3 数据库|磁盘|10-50ms|海量|全量业务数据|

## 六、实践作业：高并发读系统实现
### 1. 容器环境部署
配置项目的`Dockerfile`和`docker-compose`文件，将**数据库、后端服务、Nginx**分别用容器启动，实现环境隔离与快速部署。
### 2. 负载均衡实现
1. 后端服务启动多个实例，开启不同Rest端口（如8081、8082）；
2. Nginx（80端口）配置反向代理，尝试**轮询、加权轮询、IP哈希**等不同负载均衡算法；
3. 使用JMeter进行压力测试，观察响应时间，验证后端实例处理请求数是否均匀。
### 3. 动静分离实现
1. 编写简单前端Html文件，包含CSS、JS等静态资源；
2. Nginx中配置动静分离规则，静态资源直接处理，动态请求转发至后端；
3. 使用JMeter分别压测静态文件和后端服务，对比响应时间差异。
### 4. 分布式缓存实现
1. 引入Redis缓存，实现**商品详情页**的Cache Aside缓存模式；
2. 针对缓存穿透、击穿、雪崩问题，实现对应的解决方案（如缓存空值、分布式锁、过期时间加随机值）；
3. 压测验证缓存效果，对比开启/关闭缓存的QPS和响应时间。

三、# 分布式软件原理与技术 - 高并发设计（2）核心知识整理
## 一、缓存深度解析（进阶）
### 1. SpringBoot集成缓存实现
#### （1）本地缓存集成（Caffeine/Guava）
SpringBoot提供注解式缓存快速实现，默认使用`ConcurrentHashMap`，可配置为Caffeine/Guava等高性能本地缓存。
##### 核心注解
|注解|核心作用|
| ---- | ---- |
|`@Cacheable`|方法执行前查缓存，命中直接返回，未命中执行方法并缓存结果|
|`@CachePut`|无论缓存是否命中，执行方法并更新缓存（适用于更新操作）|
|`@CacheEvict`|清除指定缓存，可配置`allEntries=true`清空整个缓存区域|
|`@Caching`|组合多个缓存注解，实现复杂缓存逻辑|
##### 配置示例（Caffeine）
```yaml
spring:
  cache:
    type: caffeine
    caffeine:
      spec: maximumSize=500,expireAfterAccess=600s # 最大500条，访问后600秒过期
    cache-names: users,products # 预定义缓存名称
```
##### 代码示例
```java
@Service
public class UserService {
    // 查缓存，未命中查库并缓存
    @Cacheable(value="users",key="#id")
    public User getUserById(Long id){
        return findUserInDatabase(id);
    }
    // 更新后同步刷新缓存
    @CachePut(value="users",key="#user.id")
    public User updateUser(User user){
        return saveUser(user);
    }
    // 删除后清除缓存
    @CacheEvict(value ="users", key ="#id")
    public void deleteUser(Long id){
        deleteUserInDatabase(id);
    }
}
```

#### （2）分布式缓存Redis集成
SpringBoot集成Redis有**注解式**（简单场景）和**编程式**（复杂操作）两种方式，核心依赖`spring-boot-starter-data-redis`。
##### 基础配置
```yaml
spring:
  redis:
    host: localhost
    port: 6379
    password: 
    lettuce: # 连接池配置
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
  cache:
    type: redis
    redis:
      time-to-live: 600000 # 全局过期时间10分钟
      cache-null-values: true # 允许缓存空值，防御缓存穿透
```
##### 混合使用示例（注解+编程式）
```java
@Service
public class HybridService {
    @Autowired
    private RedisTemplate<String,Object> redisTemplate;
    
    // 简单查询用注解式
    @Cacheable(value = "users",key="#id")
    public User getUser(Long id){
        return userRepo.findById(id).orElse(null);
    }
    // 复杂原子操作用编程式（如计数、分布式锁）
    public void incrementScore(String userId){
        redisTemplate.opsForValue().increment("score:"+userId,1);
        redisTemplate.expire("score:"+userId,1,TimeUnit.DAYS);
    }
}
```

### 2. Redis核心进阶知识
#### （1）Redis高性能底层：epoll多路复用+Reactor模型
Redis和Nginx的高性能均基于**epoll多路复用**，配合Reactor模型处理百万级并发连接，核心是**减少线程上下文切换，最大化利用CPU**。
- **Redis**：**单线程Reactor模型**，主线程通过`epoll_wait`监听所有文件描述符（FD），就绪FD放入命令队列，单线程串行执行命令，避免锁竞争；
- **Nginx**：**多进程Reactor模型**，Master进程管理Worker进程，每个Worker进程独立运行Reactor模型，利用多核CPU，异步非阻塞处理请求。
- 核心数据结构：epoll通过**红黑树**管理所有监控的FD，**就绪链表**存储活跃FD，实现O(logN)的FD管理效率。

#### （2）Redis全数据结构及应用场景
Redis支持8种核心数据结构，每种结构针对特定场景优化，多数操作为O(1)时间复杂度，是高并发场景的核心支撑。
|数据结构|底层实现|核心操作|典型应用场景|
| ---- | ---- | ---- | ---- |
|String|动态字符串|SET/GET、INCR/DECR（原子）、SETNX（分布式锁）、MSET/MGET|缓存、计数器、分布式锁、全局ID|
|Hash|压缩列表/哈希表|HSET/HGET、HGETALL、HINCRBY|用户信息、商品详情、对象缓存|
|List|双向链表|LPUSH/RPUSH、LPOP/RPOP、BLPOP（阻塞）|消息队列、最新列表、操作日志、任务队列|
|Set|哈希表|SADD/SREM、SISMEMBER、SINTER（交集）、SUNION（并集）|共同好友、标签系统、抽奖去重、黑白名单|
|Sorted Set（ZSet）|跳跃表+哈希表|ZADD/ZREM、ZRANGE、ZINCRBY、ZSCORE|排行榜、延时队列、带权重任务调度|
|Geo|ZSet（GeoHash编码）|GEOADD、GEODIST、GEORADIUS|附近的人/店铺、LBS应用、地图服务|
|Bitmap|基于String的位操作|SETBIT/GETBIT、BITCOUNT、BITOP|用户签到、活跃用户统计、布隆过滤器|

#### （3）Redis部署模式与负载均衡
Redis有三种核心部署模式，适配不同并发规模，集群模式基于**一致性哈希**实现负载均衡和动态扩缩容。
|部署模式|适用并发|核心特点|
| ---- | ---- | ---- |
|单机模式|10万QPS以内|部署简单，无高可用，仅适用于测试/低并发场景|
|主从+哨兵（Sentinel）|10万-50万QPS|主库写、从库读，哨兵监控主库状态，故障自动切换，实现高可用|
|Redis Cluster（集群）|50万QPS以上|水平扩展，分片存储，内置主从复制，一致性哈希实现负载均衡|

##### 一致性哈希核心原理
1. 构建**2^32位哈希环**，将Redis节点（含虚拟节点）和请求Key通过哈希函数映射到环上；
2. 某个Key的请求路由到**哈希环上最近的后续节点**；
3. 引入**虚拟节点**解决物理节点负载不均问题，每个物理节点映射多个虚拟节点。
##### 核心优点
节点动态扩缩容时，**仅少量Key需要重新迁移**，最小化对系统的影响，适用于分布式缓存、CDN等场景。

## 二、缓存读写模式（SpringBoot实战实现）
三种核心读写模式的**SpringBoot代码落地**是重点，不同模式适配不同的一致性和性能需求，其中**Cache Aside**为最常用模式。

### 1. Cache Aside（旁路缓存模式）- 主流首选
#### 核心原则
- **读**：先查缓存→未命中查数据库→将数据写入缓存后返回；
- **写**：**先更新数据库，再删除缓存**（避免并发查询回写旧数据），禁止“先删缓存再更DB”；
- **删缓存失败处理**：① 重试机制+过期时间兜底；② 监听数据库binlog（Canal），独立服务异步删缓存。
#### SpringBoot实战代码
```java
@Service
public class UserCacheAsideService {
    @Autowired
    private UserRepository userRepository;

    // 读操作：Cache Aside
    @Cacheable(value="users",key="#id")
    public User getUser(Long id){
        return userRepository.findById(id).orElse(null);
    }

    // 写操作：先更DB，再删缓存（事务保证DB更新成功）
    @Transactional
    @CacheEvict(value="users",key="#user.id")
    public User updateUser(User user){
        return userRepository.save(user);
    }

    // 删除操作：先删DB，再删缓存
    @Transactional
    @CacheEvict(value="users",key="#id")
    public void deleteUser(Long id){
        userRepository.deleteById(id);
    }
}
```

### 2. Read/Write Through（缓存代理模式）
#### 核心原则
- **读**：缓存未命中时，**由缓存自行加载数据库数据**，应用无需关心加载逻辑；
- **写**：应用仅更新缓存，**缓存同步更新数据库**，二者在一个事务中，保证**强一致性**；
- 核心特点：应用与数据库解耦，由缓存层负责数据同步。
#### SpringBoot实战代码（事务保证强一致）
```java
@Service
public class UserReadWriteThroughService {
    private static final String CACHE_KEY_PREFIX = "users:";
    @Autowired
    private RedisTemplate<String, Object> redisTemplate;
    @Autowired
    private UserRepository userRepository;

    // 写操作：缓存与DB同步更新，事务保证原子性
    @Transactional
    public User updateUser(User user){
        // 1. 更新数据库
        User saved = userRepository.save(user);
        // 2. 同步更新缓存
        redisTemplate.opsForValue().set(CACHE_KEY_PREFIX + saved.getId(), saved, 30, TimeUnit.MINUTES);
        return saved;
    }

    // 删除操作：同时删除DB和缓存
    @Transactional
    public void deleteUser(Long id){
        userRepository.deleteById(id);
        redisTemplate.delete(CACHE_KEY_PREFIX + id);
    }
}
```

### 3. Write Behind（异步写回模式）
#### 核心原则
- **写**：先更新缓存，**异步批量写入数据库**，可合并多次写操作，实现“削峰填谷”；
- **核心优点**：性能极高（仅操作内存），适合高并发写场景；
- **核心缺点**：数据可能丢失（缓存宕机）、最终一致性（DB数据有延迟）；
- 适用场景：点赞计数、访问统计、购物车合并保存、物联网设备数据上报。
#### SpringBoot实战代码（Redis List+定时任务批量刷库）
```java
@Service
public class UserWriteBehindService {
    private static final String CACHE_KEY_PREFIX = "users:";
    private static final String WRITE_BEHIND_QUEUE = "user:write:queue"; // Redis List存更新ID
    @Autowired
    private RedisTemplate<String, Object> redisTemplate;
    @Autowired
    private UserRepository userRepository;

    // 写操作：更新缓存，将变更ID推入Redis List
    public User updateUser(User user){
        redisTemplate.opsForValue().set(CACHE_KEY_PREFIX + user.getId(), user, 30, TimeUnit.MINUTES);
        redisTemplate.opsForList().rightPush(WRITE_BEHIND_QUEUE, user.getId().toString());
        return user;
    }

    // 定时任务：每10秒批量从队列取数，去重后刷库（@EnableScheduling开启）
    @Scheduled(fixedDelay = 10000)
    @Transactional
    public void flushToDatabase(){
        // 批量弹出最多100条更新ID
        List<Object> userIds = redisTemplate.opsForList().leftPop(WRITE_BEHIND_QUEUE, 100);
        if (userIds == null || userIds.isEmpty()) return;

        // 去重：同一个用户多次更新仅保留最新一次
        Set<Long> uniqueIds = userIds.stream()
                .map(obj -> Long.valueOf(obj.toString()))
                .collect(Collectors.toSet());

        // 从缓存获取最新数据，批量写入数据库
        List<User> usersToSave = new ArrayList<>();
        for (Long id : uniqueIds) {
            User user = (User) redisTemplate.opsForValue().get(CACHE_KEY_PREFIX + id);
            if (user != null) usersToSave.add(user);
        }
        if (!usersToSave.isEmpty()) {
            userRepository.saveAll(usersToSave);
        }
    }
}
```

### 4. 三种缓存模式核心对比
|模式|读流程|写流程|一致性|性能|实现复杂度|典型场景|
| ---- | ---- | ---- | ---- | ---- | ---- | ---- |
|Cache Aside|应用加载DB数据至缓存|先更DB，再删缓存|强最终一致|高|低|电商商品详情、用户资料|
|Read/Write Through|缓存代理加载DB数据|缓存同步更DB（事务）|强一致|中|中|配置管理中心、游戏玩家状态|
|Write Behind|同Read Through|缓存异步批量更DB|最终一致|极高|高|点赞计数、访问统计、购物车|

## 三、缓存三大问题（穿透/击穿/雪崩）深度解决方案
### 1. 缓存穿透
#### 核心问题
请求**不存在的数据**，缓存和数据库均无，请求直接穿透到数据库，导致数据库压力激增（如QPS1万×90%穿透率=9000次/秒DB请求）。
#### 典型场景
恶意攻击（id=-1/超大id）、爬虫扫描不存在资源、业务数据尚未生成。
#### 三大解决方案对比与实现
|方案|核心原理|SpringBoot实现要点|优点|缺点|
| ---- | ---- | ---- | ---- | ---- |
|缓存空值|查询不到的数据缓存为“NULL”，设置**短过期时间**（5-10分钟）|`spring.cache.redis.cache-null-values=true`，代码判断缓存值是否为NULL|实现简单，效果直接|可能缓存大量空Key，占用内存|
|布隆过滤器|预加载所有合法Key，请求前先过滤，非法Key直接拦截|引入Guava布隆过滤器，新增数据时更新过滤器|内存占用极小，拦截彻底|实现复杂，存在**误判率**（判定存在的Key可能实际不存在）|
|参数校验|入口层校验请求参数合法性（如id>0、id在合理范围）|Controller层做参数校验，网关层增加拦截规则|简单快速，低开销|仅能拦截明显非法请求，无法应对隐蔽攻击|

##### 布隆过滤器实战代码（Guava）
```xml
<!-- 引入Guava依赖 -->
<dependency>
    <groupId>com.google.guava</groupId>
    <artifactId>guava</artifactId>
    <version>32.1.3-jre</version>
</dependency>
```
```java
@Service
public class ProductService {
    // 初始化布隆过滤器：预计100万条数据，误判率0.01
    private final BloomFilter<String> bloomFilter = BloomFilter.create(Funnels.stringFunnel(Charset.defaultCharset()), 1000000, 0.01);
    @Autowired
    private RedisTemplate<String, Object> redisTemplate;
    private static final String CACHE_KEY_PREFIX = "product:";

    // 新增商品时更新布隆过滤器
    public void addProduct(Product product) {
        // 1. 插入数据库
        // productRepository.save(product);
        // 2. 更新布隆过滤器
        bloomFilter.put(product.getId().toString());
        // 3. 清除缓存
        redisTemplate.delete(CACHE_KEY_PREFIX + product.getId());
    }

    // 查询商品：布隆过滤器防御穿透
    public String getProduct(String productId) {
        // 1. 查缓存
        String cached = (String) redisTemplate.opsForValue().get(CACHE_KEY_PREFIX + productId);
        if (cached != null) return cached;

        // 2. 布隆过滤器拦截：不存在则直接返回
        if (!bloomFilter.mightContain(productId)) {
            return "商品不存在";
        }

        // 3. 可能误判，查数据库
        String dbResult = queryDatabase(productId);
        if (dbResult != null) {
            redisTemplate.opsForValue().set(CACHE_KEY_PREFIX + productId, dbResult, 30, TimeUnit.MINUTES);
            return dbResult;
        } else {
            // 缓存空值，避免重复穿透
            redisTemplate.opsForValue().set(CACHE_KEY_PREFIX + productId, "NULL", 5, TimeUnit.MINUTES);
            return "商品不存在";
        }
    }

    private String queryDatabase(String productId) {
        // 模拟数据库查询
        return null;
    }
}
```

### 2. 缓存击穿
#### 核心问题
**单个热点Key**在失效瞬间，大量并发请求同时打穿缓存，直达数据库，导致数据库崩溃。
#### 典型场景
秒杀商品信息失效、微博热搜榜更新、爆款商品详情页。
#### 两大核心解决方案
|方案|核心原理|一致性|性能|适用场景|
| ---- | ---- | ---- | ---- | ---- |
|分布式锁|缓存失效时，仅一个线程能获取锁并查DB更新缓存，其他线程等待重试|强最终一致|有轻微锁开销|对一致性要求稍高的场景|
|逻辑永不过期|缓存不设置**物理过期时间**，仅设**逻辑过期时间**；过期时返回旧数据，异步刷新缓存|最终一致（短暂延迟）|极高（无锁）|允许短暂不一致，追求极致性能的场景|

##### 分布式锁实战代码（Redis SETNX）
```java
public Object getHotData(String key) {
    String cacheKey = "hot:" + key;
    // 1. 查缓存
    Object value = redisTemplate.opsForValue().get(cacheKey);
    if (value != null) return value;

    // 2. 获取分布式锁：SETNX + 过期时间，防止死锁
    String lockKey = "lock:" + key;
    Boolean lockSuccess = redisTemplate.opsForValue().setIfAbsent(lockKey, "1", 5, TimeUnit.SECONDS);
    if (lockSuccess) {
        try {
            // 3. 再次查缓存（双重检查），避免重复加载
            value = redisTemplate.opsForValue().get(cacheKey);
            if (value != null) return value;
            // 4. 查数据库并更新缓存
            value = queryDatabase(key);
            redisTemplate.opsForValue().set(cacheKey, value, 3600, TimeUnit.SECONDS);
        } finally {
            // 5. 释放锁
            redisTemplate.delete(lockKey);
        }
        return value;
    } else {
        // 6. 未获取锁，等待50ms后重试
        try {
            Thread.sleep(50);
        } catch (InterruptedException e) {
            Thread.currentThread().interrupt();
        }
        return redisTemplate.opsForValue().get(cacheKey);
    }
}
```

##### 逻辑永不过期核心实现
1. 定义缓存值对象，包含**业务数据**和**逻辑过期时间戳**；
2. 查缓存时，判断逻辑过期时间：未过期直接返回，过期则返回旧数据并**异步刷新缓存**；
3. 异步刷新时加分布式锁，避免多个线程同时刷新同一个Key。

### 3. 缓存雪崩
#### 核心问题
**大量Key同时过期**或**Redis集群整体故障**，导致所有请求直接打穿到数据库，造成系统瘫痪。
#### 典型场景
所有Key设置相同过期时间、系统重启后缓存清空、Redis集群宕机。
#### 五大组合解决方案（层层防御）
|解决方案|核心实现|核心作用|
| ---- | ---- | ---- |
|过期时间加随机值|基础过期时间 + 随机值（如3600s + 0-300s）|避免所有Key集体失效，基础防御策略|
|缓存预热|系统启动时，预先加载热点数据至缓存|避免冷启动空窗期，缓存为空导致的穿透|
|多级缓存|本地缓存（L1：Caffeine）+ 分布式缓存（L2：Redis）+ 数据库（L3）|Redis宕机时，本地缓存兜底，拦截大部分请求|
|限流熔断|在数据库/缓存前加限流（Sentinel/Guava RateLimiter），超出阈值则熔断|防止数据库被海量请求冲垮，最后一道防线|
|Redis高可用|主从+Sentinel/Redis Cluster部署|避免Redis单点故障，保证缓存服务持续可用|

##### 核心代码实现
1. **过期时间加随机值**
```java
int baseExpire = 3600; // 基础1小时
int randomExpire = new Random().nextInt(300); // 0-5分钟随机
redisTemplate.opsForValue().set(key, value, baseExpire + randomExpire, TimeUnit.SECONDS);
```
2. **缓存预热**（实现`ApplicationRunner`，系统启动后执行）
```java
@Component
public class CachePreloader implements ApplicationRunner {
    @Autowired
    private RedisTemplate<String, Object> redisTemplate;

    @Override
    public void run(ApplicationArguments args) {
        // 1. 从数据库加载热点Key
        List<String> hotKeys = loadHotKeysFromDB();
        // 2. 并行预热缓存
        hotKeys.parallelStream().forEach(key -> {
            Object value = queryDatabase(key);
            redisTemplate.opsForValue().set(key, value, 3600 + new Random().nextInt(300), TimeUnit.SECONDS);
        });
    }

    private List<String> loadHotKeysFromDB() {
        // 模拟加载热点Key
        return Arrays.asList("hot1", "hot2", "hot3");
    }

    private Object queryDatabase(String key) {
        // 模拟数据库查询
        return "data-" + key;
    }
}
```
3. **多级缓存**（L1本地缓存 + L2 Redis）
```java
@Service
public class MultiLevelCacheService {
    // L1 本地缓存：Caffeine，最大100条，访问后5分钟过期
    private final Cache<String, Object> localCache = Caffeine.newBuilder()
            .maximumSize(100)
            .expireAfterAccess(5, TimeUnit.MINUTES)
            .build();
    @Autowired
    private RedisTemplate<String, Object> redisTemplate;
    private static final String CACHE_KEY_PREFIX = "multi:";

    public Object getData(String key) {
        String cacheKey = CACHE_KEY_PREFIX + key;
        // 1. 查L1本地缓存
        Object value = localCache.getIfPresent(cacheKey);
        if (value != null) return value;

        // 2. 查L2 Redis缓存
        value = redisTemplate.opsForValue().get(cacheKey);
        if (value != null) {
            // 回写L1本地缓存
            localCache.put(cacheKey, value);
            return value;
        }

        // 3. 查数据库，更新L1和L2缓存
        value = queryDatabase(key);
        redisTemplate.opsForValue().set(cacheKey, value, 3600 + new Random().nextInt(300), TimeUnit.SECONDS);
        localCache.put(cacheKey, value);
        return value;
    }

    private Object queryDatabase(String key) {
        return "db-data-" + key;
    }
}
```

## 四、读写分离（数据库/Redis）
### 1. 读写分离核心概述
#### 定义
将**写操作**（INSERT/UPDATE/DELETE）路由到**主库（Master）**，**读操作**（SELECT）路由到**从库（Slave）**，通过**数据复制机制**保持主从数据最终一致。
#### 核心价值
- 适配**读多写少**业务场景（读占比80%-90%），突破单库读性能瓶颈；
- 主库专注写，避免读写锁竞争，提升写操作稳定性；
- 从库可用于备份/离线分析，主库故障时可切换从库，提升高可用；
- 低成本水平扩展：多台低配服务器扩展读能力，优于垂直升级主库。
#### 基本架构
`主库（写）→ 复制通道（binlog/WAL）→ 多从库（读）` + `请求代理/路由`（分发读写请求），常见拓扑：**一主多从**、主从级联、多主多从（极少）。

### 2. 读写分离实现方式
|实现方式|核心描述|代表工具|优点|缺点|
| ---- | ---- | ---- | ---- | ---- |
|客户端直连|在数据源/业务代码中实现路由，直接连接主从库|ShardingSphere-JDBC、Jedis/Lettuce|性能高，无额外组件开销|语言耦合，配置分散，运维复杂|
|代理中间件|独立部署代理层，对应用透明，负责请求解析和路由|ProxySQL、MyCat、Twemproxy、Codis|应用无感知，跨语言，统一管理|增加一跳网络延迟，代理可能成为瓶颈|
|数据库原生支持|数据库自带读写分离功能，无需额外组件|MySQL Router、Redis Sentinel|与数据库深度集成，简单易用|功能单一，定制性弱，扩展能力有限|

### 3. 主从数据复制模式（核心矛盾：复制延迟）
复制延迟是读写分离的核心问题，直接影响数据一致性，不同复制模式在**性能**和**一致性**之间做权衡。
|复制模式|核心原理|性能|一致性|适用场景|
| ---- | ---- | ---- | ---- | ---- |
|异步复制|主库写binlog后立即返回，从库异步拉取并重放|最高|最差（延迟最大）|对一致性要求低的场景（如新闻、资讯）|
|半同步复制|主库等待**至少一个从库**确认收到binlog后返回|中|中（保证数据不丢失）|大部分业务场景（电商商品、用户信息）|
|同步复制|主库等待**所有从库**提交事务后才返回|最差|最好（强一致）|极少使用，仅金融核心交易场景|
|组复制|多节点组成集群，基于Paxos/Raft协议实现共识，多主可写|中|高（自动协商一致性）|高可用读写分离架构（MySQL Group Replication）|

### 4. 典型实现：ProxySQL实现MySQL读写分离
ProxySQL是轻量级MySQL代理中间件，通过配置**主机组**和**查询规则**实现读写分离，核心是将写请求路由到主库主机组，读请求路由到从库主机组。
#### 核心配置
```lua
# 配置主从库主机组：主库hostgroup=10，从库hostgroup=20
mysql_servers = (
    {address="192.168.0.10",port=3306,hostgroup=10,weight=100},
    {address="192.168.0.11",port=3306,hostgroup=20,weight=100},
    {address="192.168.0.12",port=3306,hostgroup=20,weight=100}
)
# 配置应用用户
mysql_users = (
    {username="appuser", password="appuserpass",default_hostgroup=10}
)
# 配置查询规则：写请求走10，读请求走20
mysql_query_rules = (
    {rule_id=1,active=1,match_pattern="INSERT|UPDATE|DELETE",destination_hostgroup=10,apply=1},
    {rule_id=2,active=1,match_pattern="SELECT.*FOR UPDATE",destination_hostgroup=10,apply=1}, # 加锁读走主库
    {rule_id=3,active=1,match_pattern="SELECT",destination_hostgroup=20,apply=1}
)
```
#### 核心流程
1. 应用连接ProxySQL（端口6033），发送读写请求；
2. ProxySQL解析SQL，根据规则将写请求路由到主库（hostgroup=10），读请求路由到从库（hostgroup=20）；
3. 从库通过异步/半同步复制从主库同步数据。

### 5. Redis读写分离实现
基于**主从复制**实现，主库处理写请求，从库处理读请求，通过客户端/代理实现请求路由，核心是**水平扩展读能力**。
#### 核心实现方式
|方式|核心描述|代表工具|
| ---- | ---- | ---- |
|客户端直连|客户端内置读写分离逻辑，读请求分发到从库|JedisPool（多节点）、Lettuce（ReadFrom设置）|
|代理中间件|独立代理层，对应用透明，负责命令解析和路由|Twemproxy、Codis、阿里云Redis代理|
|Redis Cluster|官方集群模式，分片存储，支持`readonly`命令读取从库|Redis Cluster + 客户端配置|
#### 核心特点
- 采用**异步复制**，性能高但存在短暂数据不一致；
- 结合Sentinel/Cluster实现故障自动转移，保证高可用；
- 读请求可通过一致性哈希均匀分发到多个从库，提升并发读能力。

### 6. 读写分离最佳实践（解决复制延迟问题）
#### （1）强制读主（强一致性读）
- 核心：对一致性要求极高的操作（如用户资产、订单状态、库存），强制路由到主库；
- 实现：① SQL加注释`/* FORCE_MASTER */`，代理识别后路由；② 事务包裹读操作，事务内读默认走主库；
- 适用：金融交易、库存扣减、实时统计。

#### （2）延迟监控与剔除
- 核心：实时监控从库**复制延迟**，超过阈值（如1-5秒）自动将其从读负载池中摘除；
- 实现：ProxySQL通过`max_replication_lag`设置阈值，超过后标记为OFFLINE；自定义脚本监控`Seconds_Behind_Master`并动态调整；
- 适用：对实时性有要求，可接受短暂不可读的业务。

#### （3）智能路由
- 核心：代理层基于**SQL语义、负载、延迟**动态决策路由；
- 实现：ProxySQL支持正则规则、用户/库表级路由、延迟感知负载均衡；ShardingSphere-JDBC支持可编程式路由；
- 适用：中大型系统，需统一管理读写分离、分库分表、限流熔断。

#### （4）版本号/时间戳策略
- 核心：写操作后返回**数据版本号/时间戳**，查询时携带，代理判断从库数据是否足够新，否则路由到主库；
- 实现：利用MySQL`WAIT_UNTIL_SQL_THREAD_AFTER_GTIDS`特性，或中间件维护从库最新数据时间；
- 适用：社交动态、内容发布，可接受会话级一致性的场景。

## 五、搜索引擎（高并发读场景补充）
### 1. 搜索引擎核心原理
搜索引擎是海量数据下**快速全文检索**的核心方案，核心是**倒排索引**，替代关系数据库的低效模糊查询，工作流程分为三步：
1. **数据采集**：爬虫抓取数据，解析清洗去噪声；
2. **索引构建**：对文本分词处理，建立**倒排索引**（词项→文档列表）；
3. **查询服务**：解析用户查询，在倒排索引中匹配文档，通过BM25/TF-IDF计算相关性得分并排序返回。

### 2. 核心数据结构：倒排索引
- 定义：建立**词项 → 文档列表**的映射关系，记录词项在文档中的出现位置、频率等信息；
- 核心优点：① 检索速度快，直接通过词项定位文档；② 支持布尔查询（AND/OR/NOT）、短语查询；③ 便于相关性打分；
- 对比正向索引：正向索引是**文档→词项**，适合精确查询；倒排索引是**词项→文档**，专为全文搜索优化。

### 3. 搜索引擎 vs 关系数据库（核心差异）
|对比维度|关系数据库（MySQL）|搜索引擎（Elasticsearch）|
| ---- | ---- | ---- |
|核心索引|正向索引（B-Tree）|倒排索引（词项→文档）|
|文本匹配|`LIKE '%keyword%'`，无法利用索引，性能差|分词+相关性匹配，支持同义词、词干提取|
|查询性能|数据量大时，模糊查询性能急剧下降|性能不随数据量增长显著下降，支持海量数据检索|
|排序方式|按指定字段排序，无相关性打分|内置BM25/TF-IDF，按相关性得分排序|
|扩展性|依赖垂直扩展，分库分表侵入性强|原生水平扩展，分片+副本机制适配海量数据|

### 4. 搜索引擎在分布式软件中的典型应用
1. **站内搜索**：电商商品搜索、内容社区、企业知识库，通过Canal/DataX将DB数据同步到ES；
2. **数据库搜索加速**：采用**CQRS模式**，写走DB保证事务，读走ES实现全文检索，CDC同步数据；
3. **日志与监控分析**：ELK架构（Filebeat→Kafka→Elasticsearch→Kibana），实现日志全文搜索和可视化；
4. **推荐/广告系统**：作为召回层，快速筛选候选物品，结合机器学习模型精排；
5. **地理空间搜索**：基于Geo索引实现附近的人/店铺、LBS应用。

### 5. 关系数据库与Elasticsearch数据同步方案
|同步方案|核心原理|优点|缺点|
| ---- | ---- | ---- | ---- |
|应用双写（同步）|应用同时写入DB和ES，事务保证一致性|实现简单，数据实时一致|应用耦合度高，ES故障会阻塞DB写操作|
|应用双写（异步）|应用写DB后，通过MQ将变更发送到ES，异步消费|解耦应用与ES，不阻塞DB|存在短暂数据延迟，需保证MQ可靠性|
|Binlog订阅同步|通过Canal伪装成MySQL从库，监听binlog，异步同步到ES|完全解耦，实时性高，无应用侵入|需部署Canal/MQ，增加运维复杂度|

## 六、实践作业：高并发读综合实现
1. **分布式缓存优化**：引入Redis实现商品详情页缓存，完整处理**缓存穿透、击穿、雪崩**三大问题（要求代码落地）；
2. **MySQL读写分离搭建**：通过ProxySQL/ShardingSphere-JDBC搭建MySQL一主多从环境，在代码中测试读写分离效果，验证写主库、读从库的路由规则；
3. **Elasticsearch商品搜索（可选）**：搭建ES环境，实现商品数据从MySQL到ES的同步（Canal/binlog），开发商品关键词搜索接口，支持相关性排序。

四、# 分布式软件原理与技术 - 高并发设计（3）核心知识整理
## 一、异步与消息队列
### 1. 异步通信核心价值
异步写采用“取号离场”模式，核心是**解耦、提速、削峰**，解决同步通信的核心痛点：
|问题|同步写痛点|异步写解决方案|
| ---- | ---- | ---- |
|响应时间|需等待所有下游完成，RT达数秒|写入队列即返回，RT降至毫秒级|
|系统耦合|上游强依赖下游，一个故障全链路阻塞|解耦生产者与消费者，各自独立演进|
|流量冲击|突发流量直接打垮数据库|队列缓冲，消费者按自身能力拉取|
|资源利用率|线程阻塞等待，浪费系统资源|异步非阻塞，线程可处理其他任务|

### 2. 消息队列基础认知
#### 定义与核心模型
消息队列（Message Queue, MQ）是分布式系统中跨进程异步通信的中间件，基于**先进先出（FIFO）** 数据结构存储消息，核心有两种消息模型：
- **发布订阅模型（Topic）**：一条消息可被多个消费者消费（广播模式），适用于通知、日志分发场景；
- **点对点模型（Queue）**：一条消息仅被一个消费者消费（独占模式），适用于任务分配、订单处理场景。

#### 四大核心作用
1. **解耦**：生产者与消费者不直接依赖，通过队列隔离，修改一方不影响另一方；
2. **异步**：生产者发送消息后立即返回，无需等待消费完成，缩短主流程响应时间；
3. **最终一致性**：支持分布式事务最终一致性（如订单创建后异步发短信、加积分），通过重试和死信队列保障消息可靠处理；
4. **削峰填谷**：高峰流量在队列中排队，后端以稳定速率处理（如突发1000QPS→队列缓冲→后端平滑处理100QPS），平衡资源利用率。

### 3. 消息队列工作原理
#### 核心机制
1. **消息存储**：消息持久化到磁盘（顺序写Append-Only，性能极高），内存维护索引/缓存加速读取；
2. **消息确认（ACK）**：
   - 生产确认：生产者收到队列ACK后确认消息发送成功；
   - 消费确认：消费者处理完成后发送ACK，队列才删除消息，避免消费中断导致消息丢失；
3. **可靠性保证**：持久化（防宕机丢失）+ 副本机制（多节点同步）+ 重试/死信队列（处理失败消息）；
4. **消息顺序性**：
   - Kafka：**分区内有序**（生产者追加到分区末尾，消费者按偏移量拉取）；
   - RabbitMQ：单一队列内FIFO有序；
   - RocketMQ：支持分区顺序和全局顺序（单队列+单线程生产消费，牺牲吞吐量）。

#### 工作流程
```
生产者 → 发送消息 → 消息队列（持久化+返回ACK）→ 消费者（拉取/推送消息）→ 处理业务 → 发送消费ACK → 队列删除消息
```

### 4. 主流消息队列技术选型
|对比维度|Kafka|RabbitMQ|RocketMQ|
| ---- | ---- | ---- | ---- |
|设计定位|高吞吐流处理平台|通用消息代理|金融级分布式消息|
|吞吐量（TPS）|100万+（最高）|5-10万|50万+|
|延迟|<10ms|50-100ms|20-50ms|
|消息顺序|分区内有序|队列内有序|支持严格全局有序|
|事务消息|需外部组件|不支持|原生支持|
|定时/延迟消息|需外部组件|插件支持|原生支持|
|协议支持|Kafka协议|AMQP/MQTT/STOMP|自定义TCP|
|运维复杂度|高（需管理分区）|中|中高|
|优势|百万级吞吐，与Flink/Spark生态无缝对接|灵活路由、易用性强，支持物联网|事务+顺序+高可靠，满足金融级要求|
|典型场景|日志收集/大数据流处理|业务解耦/任务队列，物联网接入|电商交易/订单系统、支付与金融核心|

> 注：Redis的LIST/Stream可实现简单MQ，但可靠性需自行保障，慎用于核心业务。

### 5. Kafka深度解析（高吞吐核心）
#### 核心概念
- **Broker**：Kafka服务器实例；
- **Topic**：消息主题（逻辑分类），生产者向Topic发送消息，消费者从Topic订阅消息；
- **Partition**：Topic的物理分区（类似“银行窗口”），每个Partition是独立队列，决定最大消费并行度；
- **Replicas**：分区副本，保障高可用（1个Leader+多个Follower，Leader处理读写，Follower异步同步）；
- **Consumer Group**：消费者逻辑分组，组内消费者共同消费Topic，一个Partition仅分配给组内一个消费者（负载均衡）。

#### 高效读写机制
1. **顺序写+页缓存**：消息顺序追加到日志段（Segment），利用磁盘顺序写高性能；内存页缓存（Page Cache）加速读取；
2. **零拷贝**：消费数据直接从页缓存发送到网络Socket，避免内核态与用户态数据拷贝，降低CPU开销；
3. **分区并行**：多个Partition并行读写，提升整体吞吐量；
4. **消费进度管理**：消费者提交Offset（消费偏移量）到内部主题`__consumer_offsets`，重启后从上次Offset继续消费。

#### 消费语义
- **至少一次（at-least-once）**：可能重复消费，需业务幂等设计；
- **至多一次（at-most-once）**：可能丢失消息，适用于非核心场景（如日志）；
- **精确一次（exactly-once）**：Kafka 0.11+支持，需事务+幂等生产者保障。

### 6. SpringBoot集成Kafka实战
#### 核心配置（application.yml）
```yaml
spring:
  kafka:
    bootstrap-servers: localhost:9092 # Kafka服务器地址
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      acks: 1 # 仅Leader确认即成功（平衡可靠性与性能）
      retries: 3 # 发送失败重试3次
    consumer:
      group-id: seckill-group # 消费者组ID
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "*" # 信任所有包（防反序列化漏洞）
      enable-auto-commit: false # 关闭自动提交Offset，手动ACK
      auto-offset-reset: latest # 无Offset时从最新消息开始消费
    listener:
      type: batch # 批量消费
      concurrency: 3 # 并发消费者数（不超过Topic分区数）
```

#### 代码实现
1. **Topic配置**
```java
@Configuration
public class KafkaConfig {
    @Bean
    public NewTopic seckillTopic() {
        return TopicBuilder.name("seckill-orders")
                .partitions(6) // 6个分区，支持6个并发消费
                .replicas(2) // 生产环境建议2-3个副本
                .build();
    }
}
```

2. **生产者（发送秒杀订单消息）**
```java
@Service
public class OrderProducer {
    @Autowired
    private KafkaTemplate<String, Object> kafkaTemplate;

    public void sendOrder(String userId) {
        OrderMessage message = new OrderMessage(userId, System.currentTimeMillis());
        // 用userId作为Key，保证同一用户消息进入同一分区（有序）
        kafkaTemplate.send("seckill-orders", userId, message);
        System.out.println("订单消息已发送：" + userId);
    }
}

// 控制器接口
@RestController
public class SeckillController {
    @Autowired
    private OrderProducer orderProducer;

    @GetMapping("/order")
    public String seckillOrder(@RequestParam String userId) {
        // 前置校验（限流、黑名单）
        orderProducer.sendOrder(userId);
        return "订单处理中，请稍后查看结果";
    }
}
```

3. **消费者（处理订单，手动ACK+幂等）**
```java
@Service
public class OrderConsumer {
    private static final Logger logger = LoggerFactory.getLogger(OrderConsumer.class);

    @Autowired
    private OrderService orderService;
    @Autowired
    private RedisTemplate<String, Object> redisTemplate;

    @KafkaListener(topics = "seckill-orders", groupId = "seckill-group")
    public void processOrder(List<OrderMessage> messages, Acknowledgment ack) {
        logger.info("收到批量消息，数量：{}", messages.size());
        for (OrderMessage msg : messages) {
            try {
                String userId = msg.getUserId();
                String goodsId = msg.getGoodsId();
                // 幂等性检查（Redis记录已处理的用户-商品组合）
                String key = "seckill:processed:" + userId + ":" + goodsId;
                if (Boolean.TRUE.equals(redisTemplate.hasKey(key))) {
                    logger.info("用户{}已秒杀商品{}，跳过重复处理", userId, goodsId);
                    continue;
                }
                // 处理订单创建、库存扣减
                orderService.createOrder(userId, goodsId);
                // 标记为已处理
                redisTemplate.opsForValue().set(key, "1", 24, TimeUnit.HOURS);
            } catch (Exception e) {
                logger.error("处理用户{}订单失败", msg.getUserId(), e);
                // 异常不中断批量处理，后续通过重试/死信队列处理
            }
        }
        // 手动确认消费完成
        ack.acknowledge();
    }
}
```

### 7. 消息队列核心问题与解决方案
#### （1）消息重试导致乱序
- **问题**：消息消费失败重试后入队尾部，后续消息先处理（如Msg1下单重试，Msg2取消先执行）；
- **解决方案**：失败消息转入重试队列/死信队列，或数据库通过状态约束控制（如取消订单时仅更新“INIT/PAID”状态）。
```java
@Transactional
public void handleCancelOrder(OrderEvent event) {
    // 仅更新状态为INIT/PAID的订单，避免取消不存在的订单
    int rows = orderMapper.updateStatus(event.getOrderId(), "CANCEL", Arrays.asList("INIT", "PAID"));
    if (rows == 0) {
        // 取消消息先到，创建占位记录
        orderMapper.insertIgnore(new Order(event.getOrderId(), "CANCEL"));
    }
}
```

#### （2）消费者并发导致乱序
- **问题**：同一队列多消费者并行，消息顺序错乱（如Msg1下单→Thread-A，Msg2支付→Thread-B，B先完成）；
- **解决方案**：相同业务键（如orderId）的消息路由到同一Kafka分区，一个分区仅由一个消费者处理。
```java
public void sendOrderMessage(OrderEvent event) {
    // orderId作为Key，保证同一订单消息进入同一分区
    kafkaTemplate.send("order-topic", event.getOrderId(), JSON.toJSONString(event));
}
```

#### （3）消息重复与乱序（网络重传）
- **问题**：生产者超时重发导致消息重复，或消息顺序交叉；
- **解决方案**：幂等设计（如Redis记录处理序列号）+ 版本号比对（拒绝过期消息）。
```java
@Transactional
public void processSeckill(OrderEvent event) {
    // 1. 幂等性检查：序列号去重
    if (redisService.hasProcessed(event.getSequenceId())) {
        return;
    }
    // 2. 版本号比对：仅处理新版本
    int currentVersion = db.getOrderVersion(event.getOrderId());
    if (event.getVersion() <= currentVersion) {
        logger.warn("拒绝过期消息：{}", event.getVersion());
        return;
    }
    // 3. 执行业务并标记已处理
    updateDatabase(event);
    redisService.markAsProcessed(event.getSequenceId());
}
```

#### （4）消息积压与跳过
- **问题**：某条消息长时间处理失败，导致后续消息积压；
- **解决方案**：非阻塞重试（最大重试3次），失败后转入死信队列（DLT）人工干预。
```java
@RetryableTopic(
    attempts = "3", // 重试3次
    backoff = @Backoff(delay = 1000, multiplier = 2), // 退避策略（1s→2s→4s）
    topicStrategy = TopicStrategy.SINGLE_TOPIC, // 统一重试Topic
    dltStrategy = DltStrategy.FAIL_ON_ERROR // 最终失败进入DLT
)
@KafkaListener(topics = "seckill-check")
public void handle(String message) {
    verifyInventory(message); // 库存校验（可能失败）
}

// 死信队列处理
@DltHandler
public void processDlt(String message) {
    logger.error("秒杀消息彻底失败，进入死信队列：{}", message);
    // 发送告警，等待人工修复
}
```

## 二、分库分表
### 1. 分库分表核心背景
#### 为什么需要分库分表？
当单表数据量超5000万行，或单库写并发过高、连接数不足时，即使优化索引和缓存，性能仍无法满足要求，需通过“分而治之”分散压力：
- **分库**：将数据分散到多个数据库实例（不同服务器），解决单库连接数、磁盘空间瓶颈；
- **分表**：将一张大表拆分为多个结构相同的小表，解决单表数据量过大导致的查询/写入缓慢问题。

#### 分库分表分类与适用场景
|类型|定义|核心作用|适用场景|
| ---- | ---- | ---- | ---- |
|垂直分库|按业务模块拆分（如订单库、用户库、支付库）|业务解耦，分散单库压力|不同业务模块耦合严重|
|水平分库|按规则将同一业务数据拆分到多个数据库|减少单库数据量，提升并发|单库连接数不足、磁盘空间不足|
|垂直分表|将宽表中不常用/独立字段拆分到单独表（如订单表→订单基础表+订单详情表）|宽表变窄表，提升查询效率|表字段过多，部分字段访问频率低|
|水平分表|同一数据库内，按规则拆分大表为小表|减少单表数据量，加速查询|单表数据量过大（超5000万行）|

#### 分库vs分表选择原则
|场景|推荐方案|
| ---- | ---- |
|不同业务模块耦合|垂直分库|
|单库连接数/磁盘不足|水平分库|
|表字段多、访问不均|垂直分表|
|单表数据量过大|水平分表|
|高并发+大数据量|水平分库+水平分表（如4个库，每库8张表）|

### 2. 分片策略（水平分库分表核心）
#### 分片键选择三原则
分片键是分库分表的核心，需满足：
1. **离散性**：值分布均匀，避免数据倾斜（如订单状态不适合，用户ID/订单ID适合）；
2. **业务相关性**：80%以上查询携带分片键，避免全路由（扫描全部分片）；
3. **稳定性**：值不随业务变更（如手机号可能换号，用户ID/订单ID更稳定）。

#### 主流分片策略对比
|策略|数据均匀性|范围查询|扩容难度|实现复杂度|适用场景|
| ---- | ---- | ---- | ---- | ---- | ---- |
|哈希取模|★★★★★|不支持|难|★|用户信息、订单均匀分布，分片数固定|
|范围分片|★★（可能倾斜）|支持|易|★★|时序数据（日志/订单按时间）、地域分区|
|一致性哈希|★★★★|不支持|中|★★★★|动态扩缩容的缓存/数据库|
|基因分片|★★★★★|不支持|中|★★★|订单系统，需多维度查询（按订单ID/用户ID）|

#### 四大策略详解
1. **哈希取模（最经典）**
   - 原理：`分片下标 = hash(分片键) % 分片总数`（如userid=1006，4个库→1006%4=2，8张表→1006%8=6）；
   - 优点：数据分布均匀，计算简单（O(1)），同一分片键数据集中（如同一用户订单在同一张表）；
   - 缺点：扩容需重新取模，大量数据迁移。

2. **范围分片（支持范围查询）**
   - 原理：按分片键预定义区间拆分（如订单按时间：2023年表、2024年表；按ID：1-100万、101万-200万）；
   - 优点：范围查询友好（如查2024年订单直接定位分片），扩容简单（新增区间无需迁移历史数据）；
   - 缺点：数据可能倾斜（如新用户集中在某时间段）。

3. **一致性哈希（动态扩缩容）**
   - 原理：将分片键和物理分片映射到2^32位哈希环，数据顺时针找最近分片，通过虚拟节点提升均匀性；
   - 优点：扩容仅影响相邻分片，数据迁移量小；
   - 缺点：实现复杂，不支持范围查询。

4. **基因分片（多维度查询）**
   - 原理：分布式ID中嵌入“分片基因”（如用户ID后12位），路由时提取基因计算分片（如64位订单ID：时间戳41位+基因12位+序列号10位）；
   - 优点：支持多维度查询（按订单ID/用户ID均可定位分片），数据均匀；
   - 缺点：需改造ID生成器，分片数受基因位数限制（12位支持4096个分片）。

### 3. 分库分表实现模式
|模式|核心实现|代表产品|优点|缺点|
| ---- | ---- | ---- | ---- | ---- |
|客户端模式|应用内部集成分片逻辑，直连数据库|ShardingSphere-JDBC、TDDL|无额外时延，性能高|仅支持Java，侵入性强，无法减少连接数|
|代理模式|独立部署代理服务，应用连接代理，代理分发SQL|MyCat、ShardingSphere-Proxy|应用零改动，跨语言|增加网络时延，代理可能成为瓶颈|

### 4. ShardingSphere-JDBC配置示例（客户端模式）
#### 核心配置（application.yml）
```yaml
spring:
  shardingsphere:
    datasource:
      names: ds0,ds1 # 两个数据源（订单库0、订单库1）
      ds0:
        type: com.zaxxer.hikari.HikariDataSource
        driver-class-name: com.mysql.cj.jdbc.Driver
        jdbc-url: jdbc:mysql://localhost:3306/order_db0?serverTimezone=UTC&useSSL=false
        username: root
        password: 123456
      ds1:
        type: com.zaxxer.hikari.HikariDataSource
        driver-class-name: com.mysql.cj.jdbc.Driver
        jdbc-url: jdbc:mysql://localhost:3306/order_db1?serverTimezone=UTC&useSSL=false
        username: root
        password: 123456
    rules:
      sharding:
        tables:
          t_order: # 逻辑表名
            actual-data-nodes: ds$->{0..1}.t_order_$->{0..1} # 物理表分布：ds0/ds1各有t_order_0/t_order_1
            database-strategy: # 分库策略（按user_id）
              standard:
                sharding-column: user_id
                sharding-algorithm-name: db-inline
            table-strategy: # 分表策略（按order_id）
              standard:
                sharding-column: order_id
                sharding-algorithm-name: table-inline
            key-generate-strategy: # 分布式ID生成（雪花算法）
              column: order_id
              key-generator-name: snowflake
        sharding-algorithms:
          db-inline: # 分库算法（user_id%2）
            type: INLINE
            props:
              algorithm-expression: ds$->{user_id % 2}
          table-inline: # 分表算法（order_id%2）
            type: INLINE
            props:
              algorithm-expression: t_order_$->{order_id % 2}
```

### 5. 分库分表核心技术挑战与解决方案
|挑战类别|具体问题|解决方案|
| ---- | ---- | ---- |
|分布式事务|跨库操作无法保证ACID，数据不一致|XA协议（2PC）、TCC模式、Seata AT、本地消息表|
|跨库关联查询|JOIN数据分布在不同库/分片，无法直接JOIN|字段冗余、全局表（小表全库复制）、应用层组装、搜索引擎|
|分页排序|深分页（LIMIT 100000,10）需全分片拉取数据合并，性能极差|禁止深分页、游标分页（Search After）、搜索引擎|
|分布式ID生成|自增ID重复，无法用于路由|雪花算法、基因分片ID、Redis原子递增|
|数据倾斜|部分分片数据量过大，成为热点瓶颈|重新选择分片键、复合分片键、虚拟节点、热点数据隔离|
|扩容与数据迁移|取模分片扩容导致大量数据迁移|预留分片、一致性哈希、双写迁移（新旧库同时写，逐步切读）|
|跨库聚合统计|COUNT/SUM/AVG需全分片拉取数据聚合|应用层聚合、预计算（定时任务汇总）、OLAP引擎（ClickHouse）|

#### 关键问题深度解决方案
1. **跨库关联查询**
   - 优先方案：数据建模时将关联数据放在同一分片（如按用户ID分片，用户表与订单表同分片）；
   - 兜底方案：小表用“全局表”（全库复制），大表用“字段冗余”（如订单表冗余用户名），复杂查询用Elasticsearch。

2. **分页排序（深分页优化）**
   - 禁止深分页：限制用户查询前20页；
   - 游标分页（Search After）：记住上一页最后一条的排序字段（如create_time+id），下一页从该值开始查询：
     ```sql
     -- 上一页最后一条：create_time='2024-01-01 12:00:00'，id=100
     SELECT * FROM t_order 
     WHERE create_time < '2024-01-01 12:00:00' 
        OR (create_time = '2024-01-01 12:00:00' AND id > 100)
     ORDER BY create_time DESC, id DESC
     LIMIT 10;
     ```
   - 复杂场景：同步数据到Elasticsearch，由ES负责分页排序。

3. **分布式ID生成（雪花算法）**
   - 原理：生成64位长整型ID，结构为：1位符号位 + 41位时间戳（毫秒级，可用69年） + 10位机器ID（5位数据中心+5位机器） + 12位序列号（同一毫秒最多4096个ID）；
   - 核心解决：时钟回拨（回拨<5ms则等待，超5ms则抛异常）、机器ID唯一分配。

4. **数据倾斜**
   - 热点数据隔离：将爆款商品库存单独存储（如Redis+独立数据库），避免冲击分片；
   - 复合分片键：如“用户ID+商品ID”，分散热点商品的访问压力。

### 6. 数据库优化技术路线
核心原则：**能不拆则不拆**，优先通过低成本优化解决问题，优化次序为：
`主从复制/读写分离 → 垂直拆分（分库/分表） → 水平分库分表 → 引入搜索引擎/NoSQL → 数据湖/数仓`

## 三、实践作业：高并发写系统实现
1. **消息队列秒杀下单**
   - 核心流程：Redis缓存库存 → 用户秒杀请求→Nginx限流→Kafka异步接收订单→消费者处理订单创建+库存扣减；
   - 关键要求：
     - 幂等性：Redis记录“用户-商品”秒杀记录，防止重复下单；
     - 数据一致性：库存扣减采用Redis原子操作（DECR），订单创建失败则回滚库存，最终通过定时任务校验一致性；
     - 分布式ID：使用雪花算法/基因算法生成订单ID，支持按用户ID（分库）或订单ID（分表）查询。

2. **分库分表（选做）**
   - 实现工具：ShardingSphere-JDBC/Proxy；
   - 分片策略：按用户ID分库（哈希取模），按订单ID分表（哈希取模）；
   - 核心验证：跨库查询订单列表、分页排序、分布式事务（如订单创建+积分增加）。

五、分布式事务
# 分布式软件原理与技术 - 分布式事务核心知识整理
## 一、分布式事务基础认知
### 1. 分布式事务的产生背景
#### 从单体到分布式的事务困境
- **单体系统**：事务操作（如下单+扣减库存）在同一数据库中，通过本地ACID事务即可保证强一致，任一操作失败则整体回滚；
- **分布式系统**：微服务架构下，订单、库存等业务拆分到独立服务和数据库，还可能涉及Redis、ES等异构存储，跨节点操作无法通过本地事务保障一致性，出现“超卖”“少卖”等数据不一致问题。

#### 分布式环境的核心挑战
|挑战类型|具体表现|对事务的影响|
| ---- | ---- | ---- |
|节点故障|服务宕机、数据库崩溃|导致事务部分提交，无法回滚|
|网络不可靠|消息丢失、延迟、乱序|事务参与者通信失败，出现半提交状态|
|时钟漂移|节点时间不同步|破坏事务隔离性，导致读错数据版本|
|并发冲突|多事务同时操作同一资源|引发数据竞争，破坏一致性|

### 2. 分布式事务的核心目标
解决跨节点操作的**原子性**（要么全成，要么全败）和**一致性**（最终数据无矛盾），核心分为两类实现思路：
- **刚性事务**：追求强一致性，符合传统ACID特性，如2PC、3PC；
- **柔性事务**：接受短时间数据不一致，通过补偿机制保证最终一致，如TCC、SAGA、基于消息的一致性方案。

### 3. 事务选型核心原则
业务优先级决定技术选型，无需盲目追求强一致：
- **强一致优先**：银行转账、资金扣款等金融场景，宁可短暂不可用也不能错账，选刚性事务或TCC；
- **最终一致优先**：电商下单发券、日志统计等场景，优先保证核心流程可用，选SAGA或消息一致性方案。

## 二、经典分布式事务协议（刚性事务）
### 1. XA规范与2PC协议
#### 核心定义
- **XA规范**：X/Open组织定义的分布式事务处理标准，定义了事务管理器（TM）与资源管理器（RM，如数据库）的交互接口；
- **2PC协议**（Two-Phase Commit）：XA规范的核心实现，通过“准备-提交”两阶段保证所有参与者要么全提交，要么全回滚。

#### 协议流程
1. **准备阶段（投票阶段）**：
   - TM向所有RM发送“Prepare”请求；
   - 各RM执行事务操作（写Undo/Redo日志），但不提交，返回“Yes”（可提交）或“No”（不可提交）。
2. **提交/回滚阶段（决策阶段）**：
   - 若所有RM返回“Yes”，TM发送“Commit”指令，各RM提交事务并释放资源；
   - 若任一RM返回“No”或超时，TM发送“Rollback”指令，各RM通过Undo日志回滚。

#### 核心缺陷
- **单点问题**：TM故障会导致整个事务流程阻塞，无法推进；
- **同步阻塞**：RM在准备阶段需持有锁，等待TM决策，期间无法处理其他事务，吞吐量低；
- **数据不一致**：第二阶段若部分RM未收到Commit/Rollback指令，会导致部分提交、部分回滚。

### 2. 3PC协议（三阶段提交）
#### 协议流程
在2PC基础上增加“CanCommit”阶段，将“准备阶段”拆分为“CanCommit”和“PreCommit”，共三阶段：
1. **CanCommit阶段**：TM询问RM是否可执行事务（仅健康检查，不执行实际操作）；
2. **PreCommit阶段**：TM通知RM执行事务（写日志但不提交），RM返回执行结果；
3. **DoCommit阶段**：TM发送最终提交指令，RM提交事务；若超时或失败，RM自动回滚。

#### 相对2PC的改进
|特性|2PC|3PC|
| ---- | ---- | ---- |
|可用性|先执行操作再判断可用性，易阻塞|先确认可用性再执行，减少无效操作|
|阻塞风险|RM需无限等待TM指令|RM超时后自动提交，减少阻塞|
|一致性风险|TM故障可能导致部分提交|概率更低，但网络分区下仍可能不一致|

#### 局限
- 仍未完全解决一致性问题，网络分区时可能出现“部分提交”；
- 多一次网络交互，性能反而下降，实际工程中应用较少。

### 3. 现代分布式数据库的事务优化
PolarDB-X、OceanBase等现代分布式数据库基于2PC协议，通过以下方式规避传统缺陷：
1. **高可用协调者集群**：TM由单点改为多节点集群，通过Paxos/Raft选举领导者，故障自动切换；
2. **MVCC多版本并发控制**：读操作无需等待写锁，仅读取历史版本数据，压缩阻塞范围；
3. **决策日志持久化**：TM将提交决策通过Paxos/Raft同步到多数节点，避免决策丢失；
4. **多副本分片**：数据分片通过Paxos/Raft实现多副本，确保提交后数据不丢失。

## 三、现代分布式事务方案（柔性事务）
柔性事务基于BASE理论（基本可用、软状态、最终一致），将事务控制权上浮到业务层，适合微服务架构，主流方案包括TCC、SAGA、基于消息的一致性。

### 1. TCC模式（Try-Confirm-Cancel）
#### 核心思想
侵入性较强的业务层事务方案，需为每个事务参与者实现三个接口，通过“资源预留-确认提交-取消释放”保障最终一致：
- **Try**：预留业务资源（如冻结库存、锁定资金），执行可行性检查；
- **Confirm**：确认执行，扣减预留资源（如扣减库存、转账），仅在所有Try成功后执行；
- **Cancel**：取消执行，释放预留资源（如解冻库存、退还资金），任一Try失败时执行。

#### 执行流程
|场景|流程步骤|
| ---- | ---- |
|正常流程|1. TM调用所有参与者Try接口→2. 所有Try成功→3. TM调用所有参与者Confirm接口→4. 事务完成|
|异常流程|1. TM调用部分参与者Try成功→2. 某参与者Try失败→3. TM调用已成功Try的参与者Cancel接口→4. 事务回滚|

#### 关键要求
- **幂等性**：Confirm和Cancel接口需支持重复调用（如基于事务ID去重），避免重试导致错误；
- **资源隔离**：Try阶段需锁定资源，防止并发操作篡改预留资源。

#### 典型应用场景
电商下单（锁定库存+优惠券）、支付交易（冻结资金）、积分兑换（预占积分）、账户转账（冻结转出资金）。

#### 优缺点
- **优点**：性能高（锁业务资源而非库表锁），最终一致性强；
- **缺点**：业务侵入性强，开发成本高，需改造现有业务逻辑。

### 2. SAGA模式（补偿式事务）
#### 核心思想
适用于长事务场景，将分布式事务拆分为多个本地短事务（T1、T2、...、Tn），每个短事务对应一个补偿操作（C1、C2、...、Cn）：
- **正常流程**：按顺序执行T1→T2→...→Tn，事务完成；
- **补偿流程**：若Ti失败，按逆序执行C(i-1)→...→C1，补偿已成功的短事务。

#### 与TCC的核心区别
- TCC有“Try”预留阶段，先锁定资源再确认；
- SAGA无预留阶段，直接执行短事务，出错后反向补偿。

#### 优缺点
- **优点**：业务侵入性低于TCC，适合长流程、遗留系统集成；
- **缺点**：补偿逻辑复杂（需处理数据回滚、状态恢复），一致性弱于TCC。

### 3. Seata分布式事务框架
阿里开源的主流分布式事务框架，支持多种事务模式，降低分布式事务实现成本：
- **AT模式**：无侵入，基于数据源代理，解析SQL记录Undo日志，类似优化后的2PC，锁粒度更细；
- **TCC模式**：封装TCC接口调用，支持事务状态管理和重试；
- **SAGA模式**：支持状态机驱动，简化长事务补偿逻辑。

#### Seata TCC模式实战示例
1. **定义TCC接口**（商品服务+订单服务）：
```java
// 商品服务TCC接口
@LocalTCC
public interface ProductTccService {
    @TwoPhaseBusinessAction(name = "productTcc", commitMethod = "confirm", rollbackMethod = "cancel")
    boolean tryDeductStock(@BusinessActionContextParameter(paramName = "productId") Long productId,
                           @BusinessActionContextParameter(paramName = "quantity") Integer quantity,
                           BusinessActionContext context);
    boolean confirm(BusinessActionContext context);
    boolean cancel(BusinessActionContext context);
}

// 订单服务TCC接口（类似，定义tryCreateOrder/confirm/cancel）
```

2. **实现TCC接口**（商品服务）：
```java
@Service
public class ProductTccServiceImpl implements ProductTccService {
    @Autowired
    private ProductMapper productMapper;

    // Try：冻结库存
    @Override
    @Transactional
    public boolean tryDeductStock(Long productId, Integer quantity, BusinessActionContext context) {
        // 仅冻结库存，不实际扣减
        int rows = productMapper.freezeStock(productId, quantity);
        if (rows == 1) {
            context.getActionContext().put("productId", productId);
            context.getActionContext().put("quantity", quantity);
            return true;
        }
        return false;
    }

    // Confirm：确认扣减库存
    @Override
    @Transactional
    public boolean confirm(BusinessActionContext context) {
        Long productId = (Long) context.getActionContext().get("productId");
        Integer quantity = (Integer) context.getActionContext().get("quantity");
        productMapper.confirmStock(productId, quantity); // 扣减库存+解冻冻结量
        return true;
    }

    // Cancel：取消冻结库存
    @Override
    @Transactional
    public boolean cancel(BusinessActionContext context) {
        Long productId = (Long) context.getActionContext().get("productId");
        Integer quantity = (Integer) context.getActionContext().get("quantity");
        productMapper.cancelStock(productId, quantity); // 解冻冻结量
        return true;
    }
}
```

3. **Mapper接口**（数据库操作）：
```java
public interface ProductMapper {
    // 冻结库存：stock-frozen >= quantity 确保库存充足
    @Update("UPDATE product SET frozen = frozen + #{quantity} WHERE id = #{productId} AND (stock - frozen) >= #{quantity}")
    int freezeStock(@Param("productId") Long productId, @Param("quantity") Integer quantity);

    // 确认扣减：stock -= quantity，frozen -= quantity
    @Update("UPDATE product SET stock = stock - #{quantity}, frozen = frozen - #{quantity} WHERE id = #{productId} AND frozen >= #{quantity}")
    int confirmStock(@Param("productId") Long productId, @Param("quantity") Integer quantity);

    // 取消冻结：frozen -= quantity
    @Update("UPDATE product SET frozen = frozen - #{quantity} WHERE id = #{productId} AND frozen >= #{quantity}")
    int cancelStock(@Param("productId") Long productId, @Param("quantity") Integer quantity);
}
```

4. **发起全局事务**：
```java
@Service
public class OrderService {
    @Autowired
    private ProductTccService productTccService;
    @Autowired
    private OrderTccService orderTccService;

    // 全局事务注解：Seata自动管理事务生命周期
    @GlobalTransactional(name = "create-order-tcc", rollbackFor = Exception.class)
    public void createOrder(Long userId, Long productId, Integer quantity) {
        // 1. 商品服务Try：冻结库存
        boolean stockResult = productTccService.tryDeductStock(productId, quantity, null);
        if (!stockResult) throw new RuntimeException("库存不足");
        // 2. 订单服务Try：创建待确认订单
        boolean orderResult = orderTccService.tryCreateOrder(userId, productId, quantity, null);
        if (!orderResult) throw new RuntimeException("创建订单失败");
        // 所有Try成功后，Seata自动调用Confirm；失败则调用Cancel
    }
}
```

### 4. 基于消息的最终一致性
适用于高并发、低侵入性场景，核心思路：将跨服务事务拆分为本地事务+消息通信，通过消息重试保障最终一致，主流方案有“本地消息表”和“RocketMQ事务消息”。

#### 方案一：本地消息表（eBay方案）
通过本地事务将“业务操作”与“消息写入”绑定，确保消息不丢失，再通过定时任务发送消息，下游消费后执行业务。

1. **核心流程**：
   - 订单服务：本地事务（创建订单 + 写入消息表），确保订单创建和消息记录原子性；
   - 定时任务：扫描消息表，将“待发送”消息发送到MQ，发送成功后更新消息状态；
   - 库存服务：监听MQ消息，执行扣减库存（本地事务），通过Redis实现幂等去重，消费成功后手动ACK。

2. **关键代码**：
   - 订单服务本地事务（创建订单+写消息表）：
     ```java
     @Transactional
     public void createOrderWithMessage(Long userId, Long productId, Integer quantity) {
         // 1. 创建订单
         Order order = new Order(userId, productId, quantity);
         orderMapper.insert(order);
         // 2. 写入消息表（与订单创建在同一本地事务）
         MessageOutbox message = new MessageOutbox();
         message.setMessageId(UUID.randomUUID().toString());
         message.setTopic("inventory.deduct");
         message.setPayload(JSON.toJSONString(new InventoryDeductMsg(productId, quantity)));
         message.setStatus("PENDING");
         outboxRepository.save(message);
     }
     ```
   - 定时任务发送消息：
     ```java
     @Scheduled(fixedDelay = 5000) // 每5秒扫描
     public void sendPendingMessages() {
         List<MessageOutbox> pendingMessages = outboxRepository.findPendingMessages(LocalDateTime.now().minusMinutes(5));
         for (MessageOutbox message : pendingMessages) {
             try {
                 // 发送到Kafka
                 kafkaTemplate.send(message.getTopic(), message.getMessageId(), message.getPayload()).get(10, TimeUnit.SECONDS);
                 // 发送成功，更新状态
                 outboxRepository.markAsSent(message.getMessageId());
             } catch (Exception e) {
                 // 发送失败，更新重试次数
                 message.setRetryCount(message.getRetryCount() + 1);
                 outboxRepository.save(message);
             }
         }
     }
     ```
   - 库存服务消费消息：
     ```java
     @KafkaListener(topics = "inventory.deduct", groupId = "inventory-group")
     public void consume(ConsumerRecord<String, String> record, Acknowledgment ack) {
         String messageId = record.key();
         String payload = record.value();
         // 1. 幂等性检查：Redis记录已处理消息ID
         Boolean processed = redisTemplate.opsForValue().setIfAbsent("processed_msg:" + messageId, "1", Duration.ofHours(24));
         if (Boolean.FALSE.equals(processed)) {
             ack.acknowledge();
             return;
         }
         try {
             // 2. 解析消息，扣减库存（本地事务）
             InventoryDeductMsg msg = JSON.parseObject(payload, InventoryDeductMsg.class);
             inventoryService.deductStock(msg.getProductId(), msg.getQuantity());
             // 3. 消费成功，手动ACK
             ack.acknowledge();
         } catch (Exception e) {
             // 4. 消费失败，删除幂等标记，允许重试
             redisTemplate.delete("processed_msg:" + messageId);
             throw new RuntimeException("库存扣减失败，需重试", e);
         }
     }
     ```

#### 方案二：RocketMQ事务消息
利用RocketMQ原生支持的事务消息，替代“本地消息表+定时任务”，简化实现流程：
1. **发送半事务消息**：订单服务发送“半消息”（未提交）到RocketMQ，半消息不可被消费；
2. **执行本地事务**：订单服务执行本地事务（创建订单）；
3. **二次确认**：本地事务成功则提交半消息，失败则回滚半消息；
4. **事务回查**：若MQ未收到确认，定时回查订单服务事务状态，根据结果提交/回滚。

### 5. 各事务方案对比
|方案|一致性|性能|业务侵入|适用场景|典型并发能力|
| ---- | ---- | ---- | ---- | ---- | ---- |
|XA（2PC）|强一致|低|无（数据库支持）|银行核心、资金类，低并发强一致场景|1千～5千 TPS|
|TCC|最终一致（强）|高|高|交易系统、金融支付，中高并发|1万～10万 TPS|
|SAGA|最终一致|中|中|长流程、微服务链路过长、遗留系统集成|3千～1万 TPS|
|消息最终一致|最终一致（弱）|高|低|旁路逻辑（下单发券、日志）、异步解耦|10万+ QPS|
|Seata AT|最终一致|中|低|微服务无侵入需求，中等并发|5千～3万 TPS|

## 四、高并发下的一致性保障手段
### 1. 场景化一致性要求
秒杀系统中不同场景的一致性优先级不同，需差异化设计：
|场景|并发性|一致性要求|核心保障|
| ---- | ---- | ---- | ---- |
|库存扣减+订单创建|极高|高（防超卖、防重复订单）|Redis前置过滤+TCC/消息一致性|
|支付回调+订单更新|低|最终一致（幂等优先）|基于订单号幂等处理|
|订单取消+库存回滚|中等|高（防多回滚/少回滚）|乐观锁+补偿机制|
|异步旁路操作（日志/统计）|极高|低|异步消息+重试|

### 2. 高并发优化手段
|优化方向|具体做法|
| ---- | ---- |
|Redis前置过滤|热点库存存入Redis，通过Lua脚本原子扣减（防超卖、限购）|
|消息队列削峰|异步写库，避免数据库并发写压力|
|热点数据拆分|库存拆分为多个子槽位（如1000库存→10个槽位），hash分散请求，减少行锁冲突|
|幂等去重|基于Redis/数据库记录请求ID，避免重复处理|
|事务范围精简|核心流程（下单+扣库存）用事务，非核心流程（发券+日志）用异步通知|
|数据库优化|隔离级别降为READ-COMMITTED，用乐观锁替代悲观锁，确保查询条件有索引|

### 3. Redis Lua脚本原子扣减（防超卖/限购）
```lua
-- KEYS[1]：库存key，KEYS[2]：用户购买记录key
-- ARGV[1]：购买数量，ARGV[2]：用户ID，ARGV[3]：限购数量
-- 返回值：0=库存不足，1=超限，2=成功
local stock_key = KEYS[1]
local user_key = KEYS[2]
local quantity = tonumber(ARGV[1])
local user_id = ARGV[2]
local max_buy = tonumber(ARGV[3])

-- 1. 检查库存
local stock = redis.call('GET', stock_key)
if not stock or tonumber(stock) < quantity then
    return 0
end

-- 2. 检查限购
local bought = redis.call('HGET', user_key, user_id) or 0
if tonumber(bought) + quantity > max_buy then
    return 1
end

-- 3. 原子扣减库存+记录购买
redis.call('DECRBY', stock_key, quantity)
redis.call('HINCRBY', user_key, user_id, quantity)
redis.call('EXPIRE', user_key, 86400) -- 24小时过期
return 2
```

### 4. 乐观锁实现（数据库层防并发冲突）
```java
// Mapper接口：基于version字段实现乐观锁
@Update("UPDATE product SET stock = stock - #{quantity}, version = version + 1 " +
        "WHERE id = #{productId} AND stock >= #{quantity} AND version = #{version}")
int deductStockWithVersion(@Param("productId") Long productId,
                           @Param("quantity") Integer quantity,
                           @Param("version") Integer version);

// 业务层：带重试的乐观锁扣减
@Transactional
public boolean deductStockWithRetry(Long productId, Integer quantity) {
    int maxRetry = 3;
    for (int i = 0; i < maxRetry; i++) {
        // 1. 查询当前版本和库存
        Product product = productMapper.selectById(productId);
        if (product == null || product.getStock() < quantity) {
            return false;
        }
        // 2. 乐观锁更新
        int rows = productMapper.deductStockWithVersion(productId, quantity, product.getVersion());
        if (rows == 1) {
            return true; // 更新成功
        }
    }
    return false; // 重试失败
}
```

## 五、实践作业：分布式事务实现（秒杀系统）
### 1. 核心需求
订单服务与库存服务为独立微服务，实现秒杀下单的分布式事务，保障：
- 防超卖：库存扣减准确，不出现负数；
- 防重复下单：同一用户同一商品仅能秒杀一次；
- 数据一致性：下单成功则库存扣减，下单失败则库存不减少；
- 支付一致性：支付成功更新订单状态，支付失败则回滚库存。

### 2. 实现要求
1. **Redis前置过滤**：基于Lua脚本实现库存预扣减和限购，拦截无效请求；
2. **分布式事务选型**：
   - 核心流程（下单+库存扣减）：采用TCC模式或RocketMQ事务消息；
   - 支付流程（支付+订单更新）：基于消息一致性方案，实现最终一致；
3. **幂等性保障**：基于Redis/数据库记录消息ID/订单号，避免重复处理；
4. **高并发优化**：热点库存拆分、乐观锁、消息队列削峰。